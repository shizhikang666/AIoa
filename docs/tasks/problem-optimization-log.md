# Problem Optimization Log

Purpose: keep a living table of problems found during the ThinkPHP OA refactor, the practical mitigation already applied, and the next optimization to make future work faster and safer.

Update this file whenever a new recurring problem, blocker, confusing workflow, slow command, avoidable mistake, or test gap appears. Do not record secrets, local credentials, tokens, raw `.env` values, or production data.

## Update Rules

- Add a row for each meaningful problem encountered during project work.
- Keep the row concise: what happened, why it matters, root cause, mitigation, and next improvement.
- Revisit existing rows before adding duplicates; update the existing row if the same problem reappears.
- Prefer concrete commands, docs, or scripts that prevent recurrence.
- Mark status as `Open`, `Mitigated`, or `Closed`.

## Problem Table

| ID | Date | Area | Problem | Impact | Root Cause | Current Mitigation | Next Optimization | Status |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| P-001 | 2026-06-15 | Workspace orientation | The parent directory `F:\AI\projects\testJava` is not a Git repository, while the real integration repo is `OA-ThinkPHP`. | Starting from the parent path makes status, diff, and patch operations confusing. | The project uses multiple sibling worktrees plus a read-only Java source tree. | Start continuation from `F:\AI\projects\testJava\OA-ThinkPHP`; `scripts/project-progress.ps1` prints the active repo path and branch. | Keep new docs and scripts anchored to the integration repo; use explicit paths when editing from the parent workspace. | Mitigated |
| P-002 | 2026-06-15 | Search performance | Broad recursive scans include `vendor`, `target`, copied frontend assets, and old worktrees. | Simple discovery commands can time out or flood the context. | The workspace contains dependencies, build outputs, and several historical/module worktrees. | Use `rg`, targeted paths, and `scripts/project-progress.ps1` instead of dumping all files. | Add more focused helper commands if a repeated search pattern appears. | Mitigated |
| P-003 | 2026-06-15 | Worktree status | Most module worktrees are hundreds of commits behind `refactor/thinkphp-main`. | Reading old module `STATUS.md` files wastes time and can suggest stale next steps. | Work has moved into the integrated `OA-ThinkPHP` branch. | Treat `OA-ThinkPHP` as the active integration center; use optional `-IncludeWorktreeSummary` only when needed. | Prune or archive stale worktrees only after explicit user approval. | Mitigated |
| P-004 | 2026-06-15 | Continuation overhead | `STATUS.md`, `IMPLEMENT.md`, and dashboard docs are long. | New turns spend too much time re-reading history. | The refactor has many completed slices and detailed verification notes. | Added `scripts/project-progress.ps1 -Lean` and updated bootstrap docs to prefer the compact startup packet. | Keep long logs append-only but make future summaries smaller and searchable. | Mitigated |
| P-005 | 2026-06-15 | Editing workflow | A patch attempt used paths relative to the parent workspace instead of `OA-ThinkPHP`. | The first patch failed before making changes. | Tool working directory was the parent workspace while target files live in the integration repo. | Use `OA-ThinkPHP/...` paths in patches when operating from the parent, or set shell workdir to the repo. | Prefer checking `git status --short --branch` in `OA-ThinkPHP` immediately before edits. | Mitigated |
| P-006 | 2026-06-15 | Context management | Long conversations can become too dense for precise inspection before the next broad slice. | Work may slow down or rely on stale chat context instead of repository facts. | The refactor spans many modules, long status logs, and repeated verification notes. | Added `docs/tasks/context-handoff.md`; `scripts/project-progress.ps1` now points to it. | Prompt the user to open a new conversation before broad or risky work when context is overloaded. | Mitigated |
| P-007 | 2026-06-15 | Test commands | Piping a here-string into `php -r` failed because PHP expects code as the `-r` argument. | A smoke check failed before exercising the code under test. | PowerShell pipeline syntax was used for a PHP mode that does not read script code from STDIN. | Pass inline PHP through a PowerShell variable, then call `php -r $code`. | Reuse script files or tested inline-command patterns for future PHP smoke checks. | Mitigated |
| P-008 | 2026-06-15 | Runtime verification | DB-backed role selector smoke failed because MySQL at the local configured host refused the connection. | Service shape verification could not reach the database until the runtime was started. | The local runtime services were not running before the DB-backed smoke. | Added `scripts/runtime-ready.ps1` and `project-progress.ps1 -CheckRuntime` to check MySQL, Redis, and PHP FastCGI ports before DB/HTTP smokes. | Use the readiness check before every DB-backed smoke command. | Mitigated |
| P-009 | 2026-06-15 | Browser smoke readiness | Browser-side selector smoke could not proceed immediately because backend port `82` and frontend port `83` were not listening, even though the base runtime ports were ready. | Frontend-visible verification can stall while the underlying code change is already service-smokeable. | The project has separate service-bundle readiness and application-server readiness requirements; Vite can take more than a few seconds to cold-start; appending extra host/port args to `npm run dev` can also be misparsed by the existing Vite script. | Added `scripts/web-ready.ps1` and `project-progress.ps1 -CheckWeb` to check local ThinkPHP and Vue HTTP targets before browser or authenticated HTTP smokes, with a longer default HTTP timeout for Vite cold starts. | Run `.\scripts\project-progress.ps1 -CheckRuntime -CheckWeb -Lean` before browser smoke; start `php think run --host 127.0.0.1 --port 82` and plain `npm run dev` when unavailable. | Mitigated |
| P-010 | 2026-06-15 | Browser automation | The copied frontend project does not include a local Playwright dependency. | Browser smoke cannot be run as a simple project-local Node command without adding tooling or using an external browser automation capability. | Browser smoke has historically been manual/tool-assisted, while the frontend dependency set does not include Playwright. | Added `scripts/role-selector-http-smoke.ps1` as a dependency-free authenticated HTTP fallback for the role-selector dialog APIs; keep `scripts/web-ready.ps1` as the browser precondition check. | For the next true browser-smoke slice, either use an available external Playwright/browser tool or make a separate approved plan before adding a browser-test dependency. | Mitigated |
| P-011 | 2026-06-15 | HTTP smoke parsing | PowerShell `ConvertFrom-Json` failed on mixed-case alias JSON, and PHP sample output later included a leading BOM before JSON. | A smoke script can fail in the parser before reaching the behavior it is meant to verify. | PowerShell 5.1 treats JSON object keys case-insensitively, while compatibility payloads may contain both legacy uppercase and frontend aliases; strict JSON parsing also rejects a leading BOM. | Use `scripts/json-read.js` for case-sensitive JSON path reads; it now strips a leading BOM before parsing. | Keep future HTTP smokes on `json-read.js` when mixed-case aliases or shell-generated JSON are possible. | Mitigated |
| P-012 | 2026-06-15 | Verification workflow | Repeated continuation checks were spread across several commands: Git status, runtime readiness, web readiness, selector smoke, and whitespace check. | Future turns can lose time retyping commands or miss one precondition before starting a slice. | Readiness, smoke, and hygiene checks were added incrementally as separate helpers. | Added `scripts/project-preflight.ps1` to run the local preflight bundle with skip switches for unavailable layers. | Use `.\scripts\project-preflight.ps1` at the start or end of small frontend-visible/API slices; use skip switches when a layer is intentionally unavailable. | Mitigated |
| P-013 | 2026-06-15 | Commit workflow | Some continuation docs still implied committing every coherent slice. | A future Agent could commit local changes before the user explicitly asks, making review and batching harder. | Older multi-Agent process notes assumed per-slice commits, while current coordination requires explicit user or merge/coordinator approval. | Updated handoff/bootstrap/lean workflow docs and `scripts/project-progress.ps1` so commits happen only when explicitly requested or approved. | Keep old historical `STATUS.md` entries as history, but do not copy their "commit and push" wording into new active starter docs. | Mitigated |
| P-014 | 2026-06-15 | Problem-log visibility | Short `project-progress` runs could show only the start of the problem table and hide the newest problem rows. | Future Agents might miss the latest mitigations even after running the startup helper. | `project-progress.ps1` printed a configurable file head, while new problem rows are appended near the bottom of the table. | Added a `Recent Problem Rows` section that always prints the latest problem rows from `docs/tasks/problem-optimization-log.md`; `-Lean` keeps the head short while preserving recent rows. | Increase the recent-row count or add status filtering if the table grows much larger. | Mitigated |
| P-015 | 2026-06-15 | Startup context size | `.\scripts\project-progress.ps1 -SkipStatusTail` still printed a large dashboard head by default. | New conversations could spend too much context before reaching the actionable guardrails and latest problem rows. | The helper optimized away the `STATUS.md` tail but kept full default dashboard/problem head sizes. | Added `.\scripts\project-progress.ps1 -Lean`, which skips the status tail, preserves recent problem rows and commit guardrail, and now uses a dashboard summary instead of the raw table head. | Use `-Lean` as the first command in handoff/bootstrap docs; use explicit line-count params only for deeper inspection. | Mitigated |
| P-016 | 2026-06-15 | Dashboard output size | Lean startup still emitted very long dashboard table rows such as the registered-route notes. | The first command in a new conversation could still consume too much context despite using `-Lean`. | The dashboard stores dense progress facts in wide Markdown table rows that wrap heavily in terminal output. | Changed `scripts/project-progress.ps1 -Lean` to show selected dashboard metrics as compact `name: value` lines and truncate long verification notes. | Add more targeted summary patterns only when a future slice repeatedly needs a specific metric. | Mitigated |
| P-017 | 2026-06-15 | Problem-log output size | Lean startup still printed full problem table rows for recent problems, including long impact and mitigation text. | The first command could remain noisy even after dashboard summarization. | Problem rows are stored as dense Markdown tables for documentation, not terminal startup output. | Changed `scripts/project-progress.ps1 -Lean` to show problem-log path, open count, and compact recent rows with id, area, status, and problem. | Add filtering by status or area if future work needs even narrower startup output. | Mitigated |
| P-018 | 2026-06-15 | Problem-log table syntax | A problem-log cell included raw vertical bars while describing the compact row format. | Markdown table parsing and script column extraction can misread later cells such as status. | Markdown tables use vertical bars as structural separators, and the lean parser also splits on them. | Reworded the row to avoid raw vertical bars and made `Convert-ProblemLine` take the final non-empty column as status. | Avoid raw vertical bars in problem-table cells; use commas, slashes, or prose instead. | Mitigated |
| P-019 | 2026-06-15 | Workflow smoke scope | Calling `/biz/process/query/list` with empty filters can load all historic processes and variables, exhausting the local PHP memory limit. | A read-only smoke can fail by exercising an unbounded query path instead of the intended route contract. | `WorkflowQueryService::queryProcessList()` previously accepted empty filters, while Java `BizBaseProcessQueryParam` requires non-empty process keys and attributes. | `queryProcessList()` now returns `400` for missing process keys or attributes, and `scripts/workflow-read-http-smoke.ps1` asserts both a filtered success path and the empty-filter guard. | Revisit pagination only if a real Java-compatible caller needs large filtered result sets. | Closed |
| P-020 | 2026-06-15 | HTTP smoke JSON bodies | PowerShell direct `curl.exe --data $Body` can strip JSON quotes before the request reaches ThinkPHP. | Smoke scripts may report false backend failures or accidentally exercise query fallback paths instead of JSON body parsing. | External command argument parsing in Windows PowerShell is fragile for inline JSON strings. | Workflow and business read smoke POST helpers now write JSON to a temporary file and call `curl.exe --data-binary @file`. | Use temp-file JSON bodies for future PowerShell HTTP smoke POST helpers. | Mitigated |
| P-021 | 2026-06-15 | Workflow detail browser smoke | Opening a workflow detail exposed frontend crashes when cached process config was missing and when `/biz/process/variable` returned a map instead of Java variable rows. | The list pages could pass while the detail drawer still failed for copied Vue process info forms. | `useProcessParam()` assumed `SYS_USER_PROCESS_CONFIG.config` was always an array, and `ProcessController::variable()` returned the internal normalized map. | `useProcessParam()` now guards config shape, `/biz/process/variable` returns Java-compatible variable rows, and project return detail tolerates missing related project rows. | Keep browser detail clicks in workflow smoke when workflow read compatibility changes. | Mitigated |
| P-022 | 2026-06-15 | Organization selector display | The copied organization add drawer showed parent tree nodes as placeholder dashes. | Users could not confidently choose a parent organization in system or business organization forms. | `TreeBuilder::toSelector()` exposed `label` and `title`, while the copied Ant Design Vue `TreeSelect` maps its display label to `name`. | Selector nodes now include `name`, and `scripts/directory-alias-http-smoke.ps1` checks both system and business org tree selectors. | Keep frontend field mappings and backend selector alias docs in sync when adding tree selector routes. | Mitigated |
| P-023 | 2026-06-16 | Browser smoke scripting | Inline Node scripts run through Windows PowerShell can corrupt non-ASCII regular expressions before Chrome evaluates them. | Browser-smoke diagnostics can fail before testing the page and waste time on false script errors. | The PowerShell console/codepage path rewrote Chinese pattern text into invalid JavaScript regex tokens. | Promoted the reusable ASCII-only Chrome CDP helper to `scripts/browser-page-smoke.ps1`; it injects local auth/menu caches, tracks backend API calls, filters known Ant Design Vue warnings, and fails on forbidden write/provider requests. | Use the helper for repeated browser smoke slices and extend parameters only after a concrete page pattern needs it. | Closed |
| P-024 | 2026-06-16 | Browser smoke forbidden filters | Treating every `download` path as forbidden caused `/biz/customer` detail smoke to fail when the business-license image loaded through `GET /api/dev/file/download`. | Read-only browser checks can false-fail on legitimate image or blob reads. | The helper originally used a path-only forbidden regex and did not distinguish read downloads from side-effect requests. | Removed `download` from the default forbidden regex and added `-ForbiddenPathPattern` so stricter page-specific checks can opt in when needed. | Keep default browser smoke focused on write, upload, delete, approval, completion, auth/session, provider, grant, reset, and save-style paths. | Closed |
| P-025 | 2026-06-16 | Browser smoke execution | Running multiple Chrome CDP browser smokes in parallel can produce `Runtime.evaluate timeout` even when the same page passes immediately when run alone. | Parallel browser diagnostics can create false negatives and waste time rerunning pages. | Temporary Chrome instances and CDP evaluation compete for local browser/runtime resources under the same frontend/backend dev servers. | Added a named mutex to `scripts/browser-page-smoke.ps1` so helper invocations run sequentially; the failed expenditure-record smoke passed when rerun alone. | Keep browser smokes sequential unless a future dedicated browser runner manages isolation explicitly. | Closed |
| P-026 | 2026-06-16 | Browser smoke console filters | `/biz/bizpayroll` emits `Warning: [ant-design-vue: Table] width must be a number when use resizable` while still rendering and loading data successfully. | A known non-blocking UI-library warning can false-fail otherwise valid browser smoke runs. | The copied payroll table uses resizable columns with mixed width formats inherited from the frontend source. | Added this exact Ant Design Vue table warning to `scripts/browser-page-smoke.ps1` allowed-console filters; payroll rerun passed with rows and no API failures. | Revisit frontend column width cleanup only if a real UI defect appears; keep smoke focused on blocking console errors. | Closed |
| P-027 | 2026-06-16 | Browser smoke route selection | Guessing `/biz/bizdatareport` from the component folder rendered a 404, while the actual menu path is `/biz/bizdatareport/index`. | Browser smoke can false-fail before reaching the copied page when the route path is inferred from files instead of menu resources. | Dynamic routes come from `sys_resource.PATH`, and some menu paths include an explicit `/index` suffix that differs from the folder root. | Queried `sys_resource` for report menu paths and reran the smoke against `/biz/bizdatareport/index`, `/summaryStatistics`, `/settlement`, and `/saleProfit`. | When unsure, check menu `PATH` before browser smoke instead of guessing from component directories. | Closed |
| P-028 | 2026-06-16 | Browser smoke route availability | `/dev/job` exists in `sys_resource`, but the current smoke account's `loginMenu` did not include it, so the copied frontend rendered 404 even after selecting the dev module. | Browser smoke can still false-fail if it targets a menu resource that is not in the authenticated login menu used to build dynamic routes. | The copied frontend registers routes from `/sys/userCenter/loginMenu`, not every active `sys_resource` row. | Updated `scripts/browser-page-smoke.ps1` to select the module containing the target path when it exists, then limited this round to login-menu-backed paths such as `/dev/file/index`, `/dev/message/index`, `/sys/sysConfig/index`, `/sys/role`, and `/sys/user`. | Before targeting dev/sys routes, verify the path appears in `loginMenu`; do not rely only on raw `sys_resource` rows. | Closed |
| P-029 | 2026-06-16 | Browser smoke click scope | Running `-ClickFirstTableLink` on workflow pages with empty or placeholder local sample rows failed with `no visible table link found to click`, even though list rendering and APIs were healthy. | Mandatory detail-click smoke can false-fail on valid empty-state pages and hide the real pass condition. | Some copied task pages render a table empty row or no title link for the current smoke account, while the helper treats missing links as fatal when click mode is requested. | Added `-AllowMissingTableLink` to `scripts/browser-page-smoke.ps1`; strict click remains the default, but optional click now reports `click.missingAllowed=true` when no safe table link exists. | Use `-AllowMissingTableLink` only when list/empty-state rendering is an acceptable pass condition; keep strict click for pages that must open detail. | Closed |
| P-030 | 2026-06-16 | Deferred provider scope | Upload controls, cloud storage, provider sends, and physical cleanup rules were spread across API docs and status notes. | A future browser or API slice could accidentally treat a visible control as safe to click or implement before provider policy is approved. | Read-only browser coverage is complete, but upload/provider cleanup work crosses frontend smoke, file storage, credentials, external services, and cleanup semantics. | Added `docs/tasks/upload-provider-deferred-plan.md` and linked it from the API gap map with explicit forbidden-request patterns, stop conditions, controlled-deferred expectations, and implementation order. | Use the plan before upload-control browser smoke or any cloud/provider/file-cleanup implementation; keep real provider work separate from LOCAL upload compatibility. | Mitigated |
| P-031 | 2026-06-16 | Frontend API static smoke | `frontend-api-method-smoke.ps1 -ShowDeferred` reported a copied legacy API call that was inside a commented-out code block. | Deferred lists can send future work toward dead code instead of active frontend consumers. | The scanner matched API calls in raw Vue and JavaScript text without stripping HTML or JavaScript comments. | Added comment stripping before import and call scanning, preserving strings and line structure. | Keep static smoke scanners comment-aware before using their output as implementation scope. | Closed |
| P-032 | 2026-06-16 | Frontend controlled-deferred writes | Copied payment-record, return-order, and invoicing controls could reach missing API exports or unmapped write routes. | Visible controls could fail with `is not a function` or 404 before the backend could return an intentional deferred response. | The Java reference has no safe standalone write contract for these operations, while the copied frontend still exposes form/delete controls. | Added frontend API exports, protected ThinkPHP wrapper routes, and `scripts/frontend-deferred-write-wrapper-smoke.ps1`; wrappers return authenticated `code=400` without service writes or side effects. | Replace each wrapper only after a module-specific transactional write plan covers finance, return/order state, stock, invoicing, workflow, and rollback behavior. | Mitigated |
| P-033 | 2026-06-16 | Browser smoke diagnostics | Console errors that log object payloads displayed only `Object` in browser smoke output. | Detail-page failures could hide the real API error body and slow triage. | CDP console arguments for object values were recorded from `description` only. | `scripts/browser-page-smoke.ps1` now expands console object arguments with `Runtime.callFunctionOn` and waits for capture before reporting; the team-project detail rerun exposed an inaccessible sample id and then passed with a member-visible sample. | When detail smoke needs sample ids, query rows visible to the current smoke account instead of using arbitrary table rows. | Closed |
| P-034 | 2026-06-16 | Frontend route-gap scanning | Ad hoc route-gap scans undercounted covered endpoints and produced false positives for `moduleRequest`, ternary request paths, and query-only template fragments. | Stale or noisy gap counts can send the next implementation slice toward already covered or false missing routes. | The temporary scan parsed raw strings instead of frontend API wrapper structure. | Added `scripts/frontend-api-route-gap-smoke.ps1`, with `moduleRequest` support, ternary branch extraction, query-only fragment filtering, read/side-effect classification, `-ShowMissing`, `-Json`, and `-FailOnReadMissing`. | Use this script before updating `docs/tasks/api-gap-map.md` or choosing the next route slice from frontend wrapper gaps. | Closed |
| P-035 | 2026-06-16 | Preflight coverage | Frontend route-gap and controlled-deferred wrapper checks were separate commands after the 560 endpoint coverage milestone. | A future preflight could pass while route-path coverage or deferred-write behavior regressed. | The new scanners were added after `scripts/project-preflight.ps1` was created. | Added route-gap and deferred-wrapper steps to `scripts/project-preflight.ps1` with skip switches, and listed the deferred-wrapper smoke in `scripts/project-progress.ps1` fast commands. | Keep new coverage-critical smoke scripts wired into preflight with explicit skip switches. | Mitigated |
| P-036 | 2026-06-16 | HTTP smoke JSON bodies | PowerShell temp JSON files written with `Set-Content -Encoding UTF8` can include a leading UTF-8 BOM, making controller raw-body `json_decode` paths see an empty or invalid payload. | Write smokes can fail before service validation with misleading missing-field errors such as `missing userId`. | Some controllers parse `php://input` directly and did not strip a BOM before JSON decoding. | `BizUserVacationController::body()` strips the leading BOM, and the new smoke follows the established temp-file plus `curl.exe --data-binary @file` pattern. | Centralize request body parsing or make all fallback body helpers BOM-tolerant when this appears in another write controller. | Mitigated |
| P-037 | 2026-06-16 | PowerShell PHP helpers | A smoke helper wrapped `php -r` output with `[string]::Join()` and failed when the PHP cleanup command printed nothing. | A successful cleanup or setup command can fail the smoke before exercising the target API. | PowerShell command output can be `$null` when the child process exits successfully without stdout. | `scripts/dev-job-write-http-smoke.ps1` now returns an empty string when inline PHP output is null. | Reuse the null-output guard in future `Invoke-Php` helper functions. | Mitigated |
| P-038 | 2026-06-18 | Execution granularity | Route-by-route safe slices are too slow for the remaining business flows and can miss related pages, side effects, and rollback behavior. | A feature may need repeated revisits, and downstream frontend or report behavior can stay inconsistent after one isolated route is replaced. | The earlier strategy optimized for low risk while many deferred wrappers were still missing. | Active workflow now prioritizes complete feature-closure blocks with dependency maps, side-effect maps, non-goals, and end-to-end smoke plans. | Start with sale-project foundation closure, then apply the same pattern to workflow, finance, stock, provider, and remaining side-effect modules. | Mitigated |
| P-039 | 2026-06-18 | Runtime readiness | Checking the Windows `MySQL80` service can falsely report the project database as unavailable while the configured local runtime is listening on `127.0.0.1:3306`. | DB-backed smoke work may be delayed even though ThinkPHP can connect to the actual local database. | The project uses a user-provided runtime bundle and ignored `.env` credentials, not necessarily the Windows service named `MySQL80`. | Use `scripts/runtime-ready.ps1`, `scripts/project-preflight.ps1`, and app-level DB checks through ThinkPHP before treating database access as blocked. | Keep future status notes tied to the configured runtime targets from `docs/tasks/local-runtime-services.md`, not unrelated service names. | Mitigated |

## Review Checklist

Before starting a new implementation slice:

1. Run `.\scripts\project-progress.ps1 -Lean`.
2. Scan this table for `Open` problems related to the slice.
3. Update an existing row if the same problem repeats.
4. Add a new row if the problem is new and likely to recur.
5. Mention important new or changed problem rows in `STATUS.md` for the completed slice.
