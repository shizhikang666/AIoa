# Java OA To ThinkPHP Refactor Progress Dashboard

Last updated: 2026-06-27 00:00 +08:00

Agent: api-agent / test-agent

## Current Estimate

Overall production-ready completion: **80%**

Read-only API compatibility completion: **90%**

This estimate uses the final goal as the denominator: one complete runnable ThinkPHP OA system with login, RBAC, user/org, workflow, business APIs, frontend adaptation, tests, deployment, and final production data sync.

## Current Evidence

| Metric | Current Value | Notes |
| --- | ---: | --- |
| SQL tables analyzed/imported | 121 | From `F:\AI\projects\testJava\OA\oa2026.sql` |
| ThinkPHP Models | 67 | Database/model foundation is mostly in place |
| ThinkPHP Controllers | 69 | Includes auth, sys, dev, biz, mobile, gen, tenant, workflow read adapters |
| ThinkPHP Services | 60 | Includes auth/RBAC, user directory, workflow reads, and business read-only services |
| Registered route entries | 587 | Most are protected compatibility routes; public auth phone-code/WebPush and third-party auth controlled-deferred routes, public password-recovery controlled-deferred routes, protected SMS provider-send deferred wrappers, auth session/token exit, sys process-config edit, role grant saves, role add/edit/delete, sys module/menu/button/field write compatibility, mobile module/menu/button write compatibility, dev config `BIZ_DEFINE` add/edit/delete and editBatch value maintenance, dev log category delete, dev job metadata add/edit/delete and status-only job actions, gen basic metadata add/edit/delete, gen config edit/delete/editBatch, gen basic preview and ZIP download, gen config add/direct-generation controlled-deferred writes, tenant metadata add/edit/delete, workflow leave start/cancel/edit, five non-project workflow first-step starts plus cancel, project-init/project-delivery/project-reissue/project-return/project-play start/approval/cancel/reject, payment/payment-out/procurement/warehouse workflow approval side effects, and leave approve/reject transitions, sale-project foundation add/edit/history/special maintenance with copied-form product-list sync, sale-project product-item standalone add/edit/delete, sale-project visibility/specimen edit, sale-project amount edit, sale-project deal edit, sale-project cancel rollback, sale-project repeal discard, sale-project delete logical delete, sale-project invoicing add/edit/delete/complete, sale-project direct invoice add/edit/delete with reverse correction, sale-project direct reissue-order add/edit/delete, return-order add with IN delivery/inventory side effects, return-order edit/delete reverse stock/finance correction, workflow-owned project-return IN delivery/inventory side effects, ReturnAndRefund expense settlement correction, dev-file LOCAL/dynamic upload, metadata logical delete, email/SMS metadata logical delete, email/SMS provider-send deferred wrappers, and public local download, biz file-relation add/edit/delete/projectCase delete, dev-message send/delete, organization add/edit/delete, position add/edit/delete, user add/edit/delete/import/grants/status/reset-password/export-download switches, user-center self-service writes, index schedule add/delete, index all-message mark-read, customer head reassignment, sale-project product mark edits, product base add/edit/delete, product status/reconciliation, warehouse base add/edit/delete, supplier base add/edit/delete, settlement-account add/edit/status/payment-add/expenses-add/transfer-add/delete, collection-receipt direct add/edit/delete, collection-receipt mark-success, collection-receipt batch expenditure, debit-note direct add/edit/delete, debit-note mark-success, debit-note batch repayment, debit-note history add, purchase-order direct add/delete, purchase-order cancel status marker, purchase-order edit, purchase-order audit edit, purchase-order warehouse one add, purchase-order warehouse batch add, inventory add registration, inventory zero-count delete, delivery system stocktake add, payment-record direct add/delete, payment-record payer-time edit, payment-record account switch, expenditure-record direct add/delete, expenditure-record payer-time/category edit, expenditure-record account switch, payroll direct add/import-template download/CSV export/generate/import/edit/batch-edit/delete, leave-application direct add/edit/delete with annual-leave balance adjustment, vacation manual add/edit/delete, CC-record current-user add/edit/delete, sale-project draft save, customer base add/edit/delete, customer follow-up, sale-project follow-up, product-info, field-change-log, history-Excel, project-rate, team-project base add/edit/delete, team-project member add/manage/edit/delete, team-project comment/reply base writes, team-project task base/category maintenance, team-project task assignee sync, task comment add/edit/delete, and frontend controlled-deferred remaining provider/generator/task-SSE wrappers are now open |
| API compatibility docs | 90 | Stored under `docs/api` |
| Database docs | 10 | Stored under `docs/database` |
| Java Controllers in original project | 84 | Read-only reference baseline |
| Frontend API files in copied project | 76 | Static scan source: `snowy-admin-web/src/api` |
| Unique frontend API endpoints | 560 | Current `scripts/frontend-api-route-gap-smoke.ps1` normalized wrapper request paths |
| Frontend endpoints already routed | 560 | Current `frontend-api-route-gap-smoke` route-path scan; semantic completeness still depends on module-specific smoke |
| Explicit safe frontend read wrappers missing | 0 | Static scan of concrete page/list/detail/query/selector wrappers after `/auth/third/page` |
| Frontend missing read/selector candidates | 0 | `.\scripts\frontend-api-route-gap-smoke.ps1 -FailOnReadMissing` passes |
| Frontend deferred write/side-effect candidates | 0 | Static wrapper paths are now all routed; many write/action routes intentionally return controlled deferred responses until module-specific plans exist |
| Frontend baseline files copied | 908 | Copied into `snowy-admin-web`; generated/cache files excluded |
| Current branch | `refactor/thinkphp-main` | Integration branch; local commits may be ahead until the user asks to push |
| Latest frontend-slice commit | See latest Git log | Current dashboard records system resource field drawer, mobile resource browser smoke, rich-text upload browser smoke, old Editor upload fallback, and workflow read-only browser smoke |

## Recent Verification Notes

- 2026-06-27 HR/payroll direct maintenance: after explicit user product approval, `POST /biz/bizleaveapplication/add` and `POST /biz/bizpayroll/add` now leave controlled-deferred status. Leave add creates one guarded manual leave row, rejects overlapping active ranges, and deducts current-year `annualLeave` balance in the same transaction. Payroll add creates one guarded manual payroll row for an active user/org pair without generation, workflow, notification, or data-change side effects. PHP syntax lint passed for both controllers/services, `scripts/hr-payroll-direct-maintenance-http-smoke.ps1` passed for no-token/missing-body guards, leave overlap, vacation deduction, payroll org mismatch, payroll insert, and cleanup, and `scripts/frontend-deferred-write-wrapper-smoke.ps1` passed for the remaining provider/generator/task-SSE wrappers.

- 2026-06-26 payment/expenditure direct manual CRUD: after explicit user product approval, `POST /biz/bizpaymentrecord/add|delete` and `POST /biz/bizexpenditurerecord/add|delete` now leave controlled-deferred status. Adds delegate to the existing settlement-account quick income/expense creation paths. Deletes only accept guarded manual `Process_sys` rows, reject transfer and linked/protected finance categories, validate linked statement account/type/category/process/amount, logically delete the record plus statement, and reverse the stored amount against the settlement account balance. PHP syntax lint passed for both controllers/services, `php think route:list` lists the four POST routes, and `scripts/frontend-api-route-gap-smoke.ps1 -FailOnReadMissing` passes with 560/560 covered routes.

- 2026-06-26 collection/debit direct CRUD: after explicit user product approval, `POST /biz/bizcollectionreceipt/add|edit|delete` and `POST /biz/bizdebitnote/add|edit|delete` now leave controlled-deferred status. Collection receipt CRUD creates/edits/deletes standalone receipt rows bound to active payment records, rejects over-amount/over-settlement input, protects settled rows, and leaves repayment expenditure/statement creation to the batch-expenditure route. Debit note CRUD creates/edits/deletes standalone debit-note rows bound to active expenditure records, derives org from the expenditure target account on add, protects settled/history rows, and leaves loan-repayment payment/statement creation to the batch-repayment route. PHP syntax lint passed for both controllers/services, and `php think route:list` lists the six POST routes.

- 2026-06-26 purchase/inventory direct CRUD: after explicit user product approval, `POST /biz/bizpurchaseorder/add|delete` and `POST /biz/inventory/delete` now leave controlled-deferred status. Purchase-order add creates direct `NOT_COMPLETED`/`NOT_IN_WAREHOUSE` orders plus item rows from submitted supplier/product data. Purchase-order delete logically deletes only orders/items without completed settlement, warehouse state, goods expenditure, delivery records, or warehoused items. Inventory delete logically deletes only zero-count active rows after warehouse/product write-scope checks. PHP syntax lint passed for both controllers/services, route-list shows the three POST routes, and `scripts/frontend-api-route-gap-smoke.ps1 -FailOnReadMissing` still reports 560/560 covered frontend endpoints.

- 2026-06-26 environment template cleanup: `.example.env` now documents `APP_HOST`, `DB_DRIVER`, cache driver/prefix, Redis deployment keys, and release-safe `APP_DEBUG=false` guidance with placeholder secret values only. `.\scripts\deployment-readiness.ps1 -CheckEnvTemplatePolicy -SkipThinkBoot` now passes with 0 failures and 2 local warnings, and Git Bash `scripts/deployment-readiness.sh --check-env-template-policy --skip-think-boot` passes with 0 failures and 4 expected Windows-host/local warnings. Real `.env` changes, staging domain selection, Redis connectivity changes, server config edits, database writes, and production data operations remain deferred.

- 2026-06-26 CORS policy readiness: `scripts/deployment-readiness.ps1` and `scripts/deployment-readiness.sh` now include `-CheckCorsPolicy` / `--check-cors-policy`, automatically enabled by production mode, plus `-CorsProbeOrigin` / `--cors-probe-origin` for frontend-origin preflight checks. The guard scans source CORS signals, global middleware signals, wildcard origin/credential risks, frontend production API prefix shape, and optional `OPTIONS` preflight headers without printing response bodies. `scripts/project-progress.ps1 -CheckDeploy -CheckCorsPolicy -PublicBaseUrl https://api.example.com -CorsProbeOrigin https://oa.example.com -Lean` passes the guard through. Local checks passed with 0 failures; they report no global CORS middleware signal, one wildcard download-origin source match, relative frontend production API prefix, successful CORS fixture preflight, and missing CORS headers on the temporary local PHP entry. CORS header injection, server/frontend/backend config edits, reloads/restarts, `.env` changes, database writes, Java source changes, and production data operations remain deferred.
- 2026-06-26 HTTP security headers policy readiness: `scripts/deployment-readiness.ps1` and `scripts/deployment-readiness.sh` now include `-CheckSecurityHeadersPolicy` / `--check-security-headers-policy`, automatically enabled by production mode. The guard checks the `PublicBaseUrl` entry response without printing response bodies and reports HSTS, `X-Content-Type-Options`, frame protection, CSP, `Referrer-Policy`, and `Permissions-Policy` gaps. `scripts/project-progress.ps1 -CheckDeploy -CheckSecurityHeadersPolicy -PublicBaseUrl https://oa.example.com -Lean` passes the guard through. Local checks passed with 0 failures; without a URL the guard reports empty `PublicBaseUrl`, a temporary local HTTP smoke server confirmed missing-header warnings, and a local TCP fixture confirmed both scripts accept present release headers. Real staging HTTPS must confirm HSTS and the remaining headers. Response body capture, header injection, server config edits, reloads/restarts, `.env` changes, database writes, Java source changes, and production data operations remain deferred.
- 2026-06-26 web-server syntax policy readiness: `scripts/deployment-readiness.ps1` now matches the Bash readiness script with `-CheckWebServerPolicy`, `-CheckNginxSyntax`, `-CheckPhpFpmSyntax`, `-NginxBinary`, and `-PhpFpmBinary`; Bash also has `--check-web-server-policy` alongside the existing syntax flags. The guard checks Nginx/PHP-FPM command availability and can run `nginx -t` / `php-fpm -tt` without printing full config, editing vhosts, or restarting services. `scripts/project-progress.ps1 -CheckDeploy -CheckWebServerPolicy -CheckNginxSyntax -CheckPhpFpmSyntax -Lean` passes the guard through. Local checks passed with 0 failures and report that this Windows/Git Bash host does not expose `nginx` or `php-fpm`, so syntax checks must be confirmed on the actual staging host. Vhost edits, config dumps, reloads/restarts, `.env` changes, database writes, Java source changes, and production data operations remain deferred.
- 2026-06-26 runtime permission policy readiness: `scripts/deployment-readiness.ps1` and `scripts/deployment-readiness.sh` now include `-CheckRuntimePermissionPolicy` / `--check-runtime-permission-policy`, automatically enabled by production mode. The guard checks sensitive file path scope, non-public runtime path scope, `public/storage` mapping intent, backup path placement/existence, and Unix file modes on non-Windows hosts without running `chmod`, `chown`, cleanup, backup creation, or value disclosure. `scripts/project-progress.ps1 -CheckDeploy -CheckRuntimePermissionPolicy -Lean` passes the guard through. Local checks passed with 0 failures and report that `runtime/backup` still needs to be created/protected before production backups; Unix mode checks are skipped on this Windows/Git Bash host. `.env` changes, permission mutation, backup directory creation, filesystem cleanup, server/vhost edits, database writes, Java source changes, and production data operations remain deferred.
- 2026-06-26 environment template policy readiness: `scripts/deployment-readiness.ps1` and `scripts/deployment-readiness.sh` now include `-CheckEnvTemplatePolicy` / `--check-env-template-policy`, automatically enabled by production mode. The guard checks `.example.env` parseability, required runtime/cache/URL key coverage, non-local `.env` key documentation, `APP_DEBUG` release guidance, DB port shape, and secret-placeholder policy without printing values or editing env files. The Bash guard uses PHP parsing for CR-only dotenv files and fixed LF key output for Git Bash compatibility. `scripts/project-progress.ps1 -CheckDeploy -CheckEnvTemplatePolicy -Lean` passes the guard through. The first local run reported missing `DB_DRIVER`, cache/Redis keys, `APP_HOST`, and release-safe `APP_DEBUG=false` guidance; the follow-up template cleanup now resolves those template warnings. Real `.env` changes, secret disclosure, server/vhost edits, database writes, Java source changes, and production data operations remain deferred.
- 2026-06-26 release package policy readiness: `scripts/deployment-readiness.ps1` and `scripts/deployment-readiness.sh` now include `-CheckReleasePackagePolicy` / `--check-release-package-policy`, automatically enabled by production mode, plus `-ReleaseRoot` / `--release-root` for inspecting an assembled release directory. The guard checks required backend runtime entries, Composer vendor metadata, public entry files, frontend `dist` index/assets/manifest, excluded `.env`/source-control/frontend-source/dependency entries, runtime artifacts, and public-root source/config exposure without building, copying, archiving, deleting, or mutating files. `scripts/project-progress.ps1 -CheckDeploy -CheckReleasePackagePolicy -Lean` passes the guard through. Current source-root dry-runs pass with 0 failures and report expected local packaging warnings because the workspace is not a clean release root. Actual release assembly, dependency install/build execution, cleanup, server/vhost edits, `.env` changes, database writes, Java source changes, and production data operations remain deferred.
- 2026-06-26 Composer dependency policy readiness: `scripts/deployment-readiness.ps1` and `scripts/deployment-readiness.sh` now include `-CheckComposerPolicy` / `--check-composer-policy`, automatically enabled by production mode. The guard checks `composer.json`/`composer.lock` parseability, required ThinkPHP packages, `app\` PSR-4 and `extend/` PSR-0 autoload mappings, ThinkPHP post-autoload scripts, vendor/composer metadata, known `require-dev` packages installed in `vendor`, and read-only `composer validate` without running install/update/autoload-dump/vendor-publish commands. `scripts/project-progress.ps1 -CheckDeploy -CheckComposerPolicy -Lean` passes the guard through. Local checks passed with 0 failures; the local `vendor` still contains `symfony/var-dumper` and `topthink/think-trace`, so production should install dependencies with `composer install --no-dev --optimize-autoloader`. Composer install/update, dependency cleanup, `.env` edits, database writes, Java source changes, and production data operations remain deferred.
- 2026-06-25 deployment runtime readiness: `scripts/deployment-readiness.ps1` now provides a deployment preflight for required ThinkPHP files, public web-exposure guardrails, optional HTTP public-exposure probes, Git secret/artifact ignore hygiene, optional PHP error/log policy readiness, optional OPcache policy readiness, optional scheduler/queue policy readiness, optional cache/Redis policy readiness, optional cookie/session policy readiness, optional URL/HTTPS policy readiness, optional file storage policy readiness, optional provider/deferred-send policy readiness, optional database schema readiness, optional deployment artifact hygiene readiness, optional frontend build policy readiness, optional backup/restore tool readiness, hidden `.env` key presence, Composer autoload, PHP/Composer availability, PHP extensions, PHP upload/body limits, runtime/storage writable paths, public-root validation, and `php think route:list` boot. Default mode is non-destructive; `-CreateMissingWritableDirs` explicitly creates missing writable directory shells, `-CheckErrorLogPolicy` checks `display_errors`, `display_startup_errors`, `log_errors`, `error_log`, `expose_php`, and `html_errors`, `-CheckOpcachePolicy` checks OPcache extension loading plus common OPcache settings, `-CheckSchedulerPolicy` checks the scheduler/queue policy document, console command registration, app command classes, queue/worker dependency signals, and dev-job runtime controls without running jobs, `-CheckCachePolicy` checks `CACHE_DRIVER`, Redis host/port/db/timeout/password policy, and Redis TCP reachability without writing cache data or printing secrets, `-CheckCookiePolicy` checks ThinkPHP cookie/session secure, HttpOnly, SameSite, session-name, and expiry settings, `-CheckUrlPolicy` checks `APP_HOST` and `PublicBaseUrl` URL format and HTTPS policy, `-CheckStoragePolicy` checks ThinkPHP filesystem disks, public disk URL/visibility, and DevFile local upload root/exposure without upload/delete/write probes, `-CheckProviderPolicy` checks provider/deferred-send docs, guarded source/route signals for Email/SMS/WebPush/OAuth/cloud-upload deferred behavior, known Composer provider SDK package signals, and `SNOWY_SYS_DEFAULT_FILE_ENGINE` without sending messages or contacting external providers, `-CheckDatabaseSchema` checks ThinkPHP database boot, `SELECT 1`, table count, curated critical tables, and curated critical columns with read-only schema queries, `-CheckArtifactPolicy` checks release-sensitive `.git/.codex`, frontend `node_modules`, and local runtime smoke/import/build artifacts without deleting files, `-CheckFrontendBuildPolicy` checks the Vite production build script, package lock policy, `.env.production`, Vite build settings, `dist` output, `dist` source/config exposure, and frontend temporary build artifacts without running npm install/build, `-Production` enables those runtime guards and fails on public error display/header exposure issues, missing/disabled OPcache, undocumented worker/job signals, required Redis settings/reachability failures, production-unsafe cookie policy, configured non-local non-HTTPS URLs, unsafe file storage policy, undocumented/unsafe provider enablement, database schema baseline failures, release-sensitive local artifacts, or unsafe frontend build policy, `-CheckBackupTools` checks dump/restore command availability, DB `.env` inputs, and backup directory writability without dumping data or printing secrets, `-Production` also enables that backup guard automatically, `-ExpectedPublicRoot` verifies that a known document-root path resolves to this project's `public` directory, `-PublicBaseUrl` probes sensitive paths such as `/.env`, `/composer.json`, `/vendor/autoload.php`, `/app`, `/config`, `/docs`, and `/scripts` without printing response bodies, and `-MinUploadMaxFilesize` / `-MinPostMaxSize` tune upload/body warning thresholds. The public exposure guard fails if source/config/dependency entries such as `.env`, `vendor`, `app`, `config`, `route`, `docs`, or `scripts` are found under `public`, the HTTP exposure guard fails on 2xx responses for sensitive paths and warns on redirects/probe failures, and the Git guard fails if `.env` is tracked while warning on missing ignore rules for `.env`, `vendor`, `runtime`, or `public/storage` artifacts. The PHP upload/body guard fails if uploads are disabled or `max_file_uploads` is invalid, and warns on low `upload_max_filesize`, low or inconsistent `post_max_size`, or `memory_limit` below `post_max_size`. `scripts/deployment-readiness.sh` provides the Linux staging/production counterpart with optional Nginx/PHP-FPM syntax checks and matching public-root/exposure/Git/error-log/OPcache/scheduler/cache/cookie/url/storage/provider/database/artifact/frontend/backup/upload validation. `public/storage/.gitignore` keeps public-disk upload artifacts out of Git, `scripts/project-progress.ps1 -CheckDeploy` can include the check in the normal progress snapshot and pass through `-CheckErrorLogPolicy`, `-CheckOpcachePolicy`, `-CheckSchedulerPolicy`, `-CheckCachePolicy`, `-CheckCookiePolicy`, `-CheckUrlPolicy`, `-CheckStoragePolicy`, `-CheckProviderPolicy`, `-CheckDatabaseSchema`, `-CheckArtifactPolicy`, `-CheckFrontendBuildPolicy`, `-CheckBackupTools`, `-MysqlDumpBinary`, `-MysqlClientBinary`, `-BackupDirectory`, `-SchedulerPolicyDocument`, `-CacheTcpTimeoutSeconds`, `-ExpectedPublicRoot`, `-PublicBaseUrl`, `-HttpProbeTimeoutSeconds`, `-MinUploadMaxFilesize`, and `-MinPostMaxSize`, and `docs/tasks/deployment-server-checklist.md` records the later Nginx/PHP-FPM rehearsal checklist. Nginx/PHP-FPM vhost edits, queue/scheduler execution, database migrations/schema changes, artifact cleanup/deletion, frontend dependency install/build execution, cloud/provider credential validation, backup automation, production data operations, and final online data sync remain deferred.
- 2026-06-25 sale-project invoice direct add/edit/delete: `POST /biz/saleprojectinvoice/add` now creates one direct delivery invoice and linked invoice items for active non-`FOLLOW` sale projects, `POST /biz/saleprojectinvoice/edit` updates only logistics/master fields, and `POST /biz/saleprojectinvoice/delete` logically deletes the invoice plus invoice items while reversing project-product `DELIVERY`/`STATE` and recalculating sale-project shipment state. Direct edit/delete reject workflow-owned invoices with active `delivery_record` rows, so stock, finance, workflow, notification, file cleanup, and Java data-change side effects remain deferred. `scripts/sale-project-invoice-add-http-smoke.ps1` passed against `http://127.0.0.1:82`.
- 2026-06-25 sale-project reissue-order direct add/edit/delete: `POST /biz/saleprojectreissueorder/add` creates one active direct reissue order with linked `REISSUE_ORDER`/`WAIT_DELIVER` project-product rows and child relations; `POST /biz/saleprojectreissueorder/edit` replaces direct order master fields and product rows only while the linked reissue items are undelivered/unreferenced; `POST /biz/saleprojectreissueorder/delete` logically deletes direct reissue order/product/relation rows and restores sale-project totals/status. Direct add/edit/delete reject workflow-owned process ids by `act_hi_procinst.PROC_INST_ID_`. Delivery invoice, inventory, finance, workflow, notification, file cleanup, and Java data-change side effects remain deferred. `scripts/sale-project-reissue-order-add-http-smoke.ps1` passed against `http://127.0.0.1:82`.
- 2026-06-25 return-order reverse stock/finance correction: direct `POST /biz/returnorder/edit` now validates edited payloads first, protects workflow-owned rows, reverses active `ReturnAndRefund` expenditure/statement/account side effects, reverses active return IN delivery/inventory side effects, updates master/detail rows, rebuilds return IN delivery/inventory rows, and recalculates affected sale-project totals. Direct `/delete` performs the same reverse correction before logical delete and total restoration. `scripts/return-order-write-http-smoke.ps1` passed against `http://127.0.0.1:82`.
- 2026-06-25 workflow project return auto refund: `Process_sale_project_product_return` approval now creates an automatic `ReturnAndRefund` expenditure and settlement-account statement when the sale project has an active `ACCOUNT_ID`. The side effect reuses the settlement-account expense path, so the selected account is decremented, the return order becomes `AlreadySettled`, and sale-project return totals are recalculated in the same approval transaction. No-account auto-refund remains deferred. `scripts/workflow-project-return-approve-http-smoke.ps1` passed against `http://127.0.0.1:82`.
- 2026-06-25 return-order inventory/refund settlement: direct `POST /biz/returnorder/add` now writes Java-compatible return IN `delivery_record` rows and increments inventory in the same transaction. `Process_sale_project_product_return` approval now increments inventory for its IN delivery rows, and later gained automatic `ReturnAndRefund` creation for projects with configured settlement accounts. `POST /biz/settlementaccount/expenses/add` now applies Java-compatible `ReturnAndRefund` correction by updating return-order settlement state, rejecting over-refund rollback, and recalculating sale-project return totals. `scripts/return-order-write-http-smoke.ps1`, `scripts/workflow-project-return-approve-http-smoke.ps1`, and `scripts/settlement-account-expenses-add-http-smoke.ps1` passed against `http://127.0.0.1:82`.
- 2026-06-25 sale-project standalone product item: `POST /biz/saleprojectproductitem/add`, `/edit`, and `/delete` now provide protected standalone maintenance for visible `FOLLOW` sale projects. The implementation reuses the normal sale-project product-list validation rules for enabled products, kit child relations, logical delete, and reference protection. Active invoice/return references or delivered quantities block protected edits/deletes; non-`FOLLOW` project mutation, delivery, invoice, inventory, finance, workflow, and Java event-bus side effects remain deferred. `scripts/sale-project-product-item-standalone-http-smoke.ps1` passed against `http://127.0.0.1:82`.
- 2026-06-25 workflow project return approval: `POST /biz/process/project/return/start` now creates `Process_sale_project_product_return` first-step runtime/history rows. Cancel and reject close without return-order, inventory, or finance side effects; approval replaces `BizSaleProjectReturnProductApproveDelegate` for the bounded return path by writing one `return_order`, one `return_order_item`, Java-compatible IN `delivery_record` rows, inventory increments, optional auto-refund expenditure/statement rows when the project has `ACCOUNT_ID`, and recalculated sale-project return totals. Kit relations decompose returned project items into child-product delivery rows. Invoice, payment, and project status fields stay unchanged during return approval itself. `scripts/workflow-project-return-approve-http-smoke.ps1` passed against `http://127.0.0.1:82`.
- 2026-06-25 workflow project reissue approval: `POST /biz/process/project/reissue/start` now creates `Process_project_reissue_product` first-step runtime/history rows. Cancel and reject close without reissue-order or stock/finance side effects; approval replaces `BizSaleProjectReissueProductApproveDelegate` for the bounded reissue path by writing one `biz_sale_project_reissue_order`, one appended `biz_sale_project_product_item` with `CATEGORY = REISSUE_ORDER`, `STATE = WAIT_DELIVER`, and `PROJECT_REISSUE_ORDER_ID`, plus one child `sale_project_product_item_relation`. Sale-project amount/status, stock, invoice, payment, expenditure, and settlement rows stay unchanged. `scripts/workflow-project-reissue-approve-http-smoke.ps1` passed against `http://127.0.0.1:82`.
- 2026-06-22 workflow project delivery approval: `POST /biz/process/project/delivery/start` now creates `Process_sale_project_delivery` first-step runtime/history rows. Cancel and reject close without invoice or stock side effects; approval replaces `BizSaleProjectDeliveryApproveDelegate` for the bounded delivery path by writing `biz_sale_project_invoice`, `biz_sale_project_invoice_item`, and OUT `delivery_record` rows with `PROCESS_ID = processInstanceId`, decrementing inventory, updating product-item `DELIVERY`/`STATE`, and recalculating sale-project `PROJECT_STATE`. `scripts/workflow-project-delivery-approve-http-smoke.ps1` passed against `http://127.0.0.1:82`.
- 2026-06-22 workflow project play approval: `POST /biz/process/project/play/start` now creates `Process_sale_project_play` first-step runtime/history rows. First approval advances to BPMN-compatible `Activity_payment_approval`; finance reject closes without payment side effects; finance approve replaces `BizSaleProjectPlayStateApproveDelegate` for the bounded project collection path by writing `settlement_account_statement` and `biz_payment_record` rows with `PROCESS_ID = processInstanceId`, `PROCESS_CATEGORY = Process_sale_project_play`, and `SETTLEMENT_CATEGORY = PROJECT_PLAY`, then recalculating sale-project `AMOUNT_COLLECTED`, `PLAY_STATE`, and `PROJECT_STATE`. `scripts/workflow-project-play-approve-http-smoke.ps1` passed against `http://127.0.0.1:82`.
- 2026-06-22 workflow project init approval: `POST /biz/process/project/init/start` now marks `FOLLOW` sale projects as `PENDING_APPROVAL` and creates `Process_sale_project_init` first-step runtime/history rows. Cancel and reject roll the project back to `FOLLOW`; approval replaces `BizSaleProjectInitStateApproveDelegate` for the bounded initial-order path by writing sale-project delivery/account/amount fields, product items, `SALE_PROJECT` file relations, optional invoicing rows, customer deal amount, and `PROCESS_ID = processInstanceId`. `scripts/workflow-project-init-approve-http-smoke.ps1` passed against `http://127.0.0.1:82`.
- 2026-06-22 workflow payment approval: `Process_payment` `Activity_approval` approve now replaces the Java payment-in delegate for the bounded settlement-account income path. It closes workflow runtime/history rows and calls the PHP settlement-account service so statement/payment rows use `PROCESS_ID = processInstanceId`, statement `PROCESS_CATEGORY = Process_payment`, and the selected settlement account balance increments; reject closes without finance side effects. `scripts/workflow-payment-approve-http-smoke.ps1` passed against `http://127.0.0.1:82`, and `scripts/settlement-account-payment-add-http-smoke.ps1` confirms manual quick income still writes `Process_sys`.
- 2026-06-22 workflow payment-out approval: `Process_reimbursement` and `Process_make_payment` now follow the BPMN two-step path: first `Activity_approval` approve creates an active `Activity_pay_approval` finance task without business rows, and finance approval calls the PHP settlement-account service to create expense statement/expenditure rows with `PROCESS_ID = processInstanceId`, statement `PROCESS_CATEGORY` equal to the process key, and account balance decrement. `scripts/workflow-payment-out-approve-http-smoke.ps1` passed for both process keys, and `scripts/settlement-account-expenses-add-http-smoke.ps1` confirms manual quick expense still writes `Process_sys`.
- 2026-06-22 workflow procure approval: `Process_procure` now follows the staged BPMN path: leader approval creates `Activity_procure_approval`, procurement confirmation persists submitted `productList`/`amount`, optional `Activity_approval_procure` general-office approval is honored, and final approval creates `biz_purchase_order` plus `biz_purchase_order_item` rows with `INSTANCE_ID = processInstanceId`, `SETTLEMENT_STATUS = NOT_COMPLETED`, and `STORAGE_STATUS = NOT_IN_WAREHOUSE`. Reject at any stage closes without purchase-order side effects. `scripts/workflow-procure-approve-http-smoke.ps1` passed against `http://127.0.0.1:82`.
- 2026-06-22 workflow procure warehouse approval: `Process_procure_in_warehouse` `Activity_approval` approve now replaces the Java warehouse delegate for the bounded purchase-order stock-in path. It closes workflow runtime/history rows and calls the PHP purchase-order warehouse-in service so delivery rows use `PROCESS_ID = processInstanceId`, `PROCESS_CATEGORY = Process_procure_in_warehouse`, and `OBJECT_ID = orderId`; order/items move to `IN_WAREHOUSE`, and inventory is incremented. `scripts/workflow-procure-warehouse-approve-http-smoke.ps1` passed against `http://127.0.0.1:82`.
- 2026-06-22 workflow general start runtime: `POST /biz/process/payment/start`, `/reimbursement/start`, `/makePayment/start`, `/procure/start`, and `/procure/warehouse/start` now create minimal first-step runtime/history rows, active `Activity_approval` tasks, variables, optional CC rows, and optional workflow file relations. `scripts/workflow-general-start-http-smoke.ps1` passed against `http://127.0.0.1:82`, including missing-amount validation, activity detail/query readback, payment fake-account approve rollback, payment-out first approval into finance task plus finance reject, procurement first approval into procurement confirmation plus procurement reject, procurement-warehouse fake-order approve rollback, cancel cleanup, final cancel variables, and unchanged payment/expenditure/purchase/delivery business-table counts.
- 2026-06-22 settlement-account delete: `POST /biz/settlementaccount/delete` now performs protected logical deletion for unused accounts after full-batch tenant/write-scope validation and active reference checks against settlement statements, payment records, and expenditure records. `scripts/settlement-account-delete-http-smoke.ps1` passed against the local backend at `http://127.0.0.1:82`; balances, finance rows, workflows, notifications, Java source, schema, and production data operations remain untouched.
- 2026-06-22 workflow payroll generation coverage: `scripts/workflow-task-transition-http-smoke.ps1` now cross-checks that an approved `Process_ask_leave` `leaveOfAbsence` row is later consumed by `/biz/bizpayroll/generate/add`, with generated `biz_payroll.VACATION` matching the workflow leave amount. This matches Java's generation-time payroll calculation; automatic updates to already-created payroll rows remain deferred.
- 2026-06-22 workflow file relation binding: active `POST /biz/process/leave/start` now mirrors the file-relation part of Java `CopyUserDelegate` for `Process_ask_leave`, generating one `biz_file_relation` row per active submitted `fileIdList` item, returning `fileRelationCount`, and reading back through `/biz/process/fileList`. `scripts/workflow-leave-start-http-smoke.ps1`, `scripts/workflow-read-http-smoke.ps1`, `scripts/test-agent-smoke.ps1 -SkipComposer -BackendBaseUrl http://127.0.0.1:82 -FileRelationHttpSmoke`, and `scripts/workflow-task-transition-http-smoke.ps1` passed; copy/file generation for other process keys, notifications, automatic existing-payroll row updates, and SSE push remain deferred.
- 2026-06-22 workflow copy-user CC records: active `POST /biz/process/leave/start` now mirrors the CC-record part of Java `CopyUserDelegate` for `Process_ask_leave`, generating one `biz_cc_records` row per submitted `copyUserIdList` user and returning `ccRecordCount`. `scripts/workflow-leave-start-http-smoke.ps1` verified generated-row DB state, `/biz/ccrecords/page` readback, and cleanup; active leave-start file relation binding is covered by the later workflow file-relation slice, while notifications, other process keys, automatic existing-payroll row updates, and SSE push remain deferred.
- 2026-06-22 direct leave annual-leave adjustment: direct `POST /biz/bizleaveapplication/edit` now adjusts the locked current-year vacation balance by the old/new annual-leave delta, direct `/delete` restores annual-leave amounts before logical delete, and insufficient-balance/underflow failures roll back the leave write. `scripts/biz-leave-application-vacation-adjustment-http-smoke.ps1` passed against the local backend at `http://127.0.0.1:82`; direct leave add, vacation generation, automatic existing-payroll row updates, copy-user generation outside active leave start, notifications, and data-change events remain deferred.
- 2026-06-22 workflow process cancel/edit runtime: replaced `POST /biz/process/cancel` and `/leave/edit` controlled-deferred behavior with minimal active `Process_ask_leave` runtime writes. Cancel now closes unapproved active leave workflows with final `cancel` variables and no leave/vacation side effect; edit now updates editable `endTime`/`amount`/`remark` variables once and approval later uses the edited values. `scripts/workflow-process-cancel-edit-http-smoke.ps1` passed against the local backend at `http://127.0.0.1:82`; other starts/transitions, non-leave delegates, copy-user generation outside active leave start, automatic existing-payroll row updates, and SSE push remain deferred.
- 2026-06-22 workflow annual-leave deduction: approved `annualLeave` workflows now lock the current-year `biz_user_vacation` balance, validate remaining days, increment `USED_AMOUNT`/`VERSION`, and roll the whole transition back on insufficient balance. `scripts/workflow-task-transition-http-smoke.ps1` passed against the local backend at `http://127.0.0.1:82`; copy-user generation outside active leave start, automatic existing-payroll row updates, other starts/transitions, non-leave delegates, and SSE push remain deferred. Direct leave-row restoration is covered by the later direct leave annual-leave adjustment slice.
- 2026-06-22 workflow leave application side effect: approved `Process_ask_leave` `Activity_approval` transitions now generate/read back one `biz_leave_application` row from historic workflow variables, while rejected leave transitions create zero leave rows. `scripts/workflow-task-transition-http-smoke.ps1` passed against the local backend at `http://127.0.0.1:82`; copy-user generation outside active leave start, automatic existing-payroll row updates, other starts/transitions, non-leave delegates, and SSE push remain deferred.
- 2026-06-22 workflow task transition runtime: replaced `POST /biz/task/approve` and `/reject` controlled-deferred behavior with minimal `Process_ask_leave` `Activity_approval` transitions. It validates current assignee ownership, writes `AGREE`/`REJECT` history variables, closes task/activity/process history rows, deletes runtime task/variable/execution rows, and reads back through history task pages. `scripts/workflow-task-transition-http-smoke.ps1` and `scripts/frontend-deferred-write-wrapper-smoke.ps1` passed against the local backend at `http://127.0.0.1:82`; other process/task transitions, other starts, non-leave BPMN delegates, copy-user generation outside active leave start, automatic existing-payroll row updates, and SSE push remain deferred.
- 2026-06-22 workflow leave start runtime: replaced `POST /biz/process/leave/start` controlled-deferred behavior with a minimal transitional `Process_ask_leave` runtime/history write. It validates the token user, tenant/org context, category, start time, and approver/copy/file user ids; creates root/approval executions, one active approval task, runtime variables, matching history rows, active copy-user CC rows, and active workflow file relations; reads back through `/biz/task/page`, `/biz/process/page`, `/biz/ccrecords/page`, `/biz/process/fileList`, and `/biz/task/runtime/activity/detail`; and cleans up smoke rows by process instance id. `scripts/workflow-leave-start-http-smoke.ps1` and `scripts/frontend-deferred-write-wrapper-smoke.ps1` passed against the local backend at `http://127.0.0.1:82`; other starts, non-leave BPMN delegates, automatic existing-payroll row updates, and broader workflow completion remain deferred. Approved leave-row creation is covered by the later workflow leave application side-effect slice.
- 2026-06-18 sale-project product-list mutation smoke: extended `/biz/saleproject/add` and `/edit` so copied-form `productList` arrays now maintain `biz_sale_project_product_item` and `sale_project_product_item_relation` rows in the existing project transaction. Omitted or `null` `productList` preserves rows; submitted arrays validate enabled tenant/data-scope products, require positive quantities and non-negative money fields, set item rows to `INIT`/`WAIT_DELIVER`, auto-hydrate kit-product children from `product_relation`, logically delete removed rows, and block product/quantity/money/child changes or deletion for items referenced by active invoice/return rows. `scripts/sale-project-product-item-mutation-http-smoke.ps1` passed against the local backend at `http://127.0.0.1:82`.
- 2026-06-18 return-order write smoke: replaced `/biz/returnorder/add`, `/edit`, and `/delete` controlled-deferred behavior with direct return-order master/detail maintenance. This was later extended on 2026-06-25 so direct add writes return IN delivery records and increments inventory, direct edit/delete reverse active return IN delivery/inventory and `ReturnAndRefund` finance side effects, `ReturnAndRefund` expenses correct settlement state through the settlement-account expense path, and workflow project-return approval auto-creates `ReturnAndRefund` rows when the project has a configured account. No-account auto-refund, notifications, and Java event bus behavior remain deferred. `scripts/return-order-write-http-smoke.ps1` passed against the local backend at `http://127.0.0.1:82`.
- 2026-06-18 sale-project invoicing write smoke: replaced `/biz/saleprojectinvoicing/add`, `/edit`, and `/delete` controlled-deferred behavior with narrow invoice-application row maintenance. It validates project visibility through tenant/data-scope guards, enforces Java add/edit field requirements and ticket/state enums, forces add rows to `INVOICING_STATE_WAIT`, supports edit state updates, logically deletes after full-batch validation, and keeps sale-project, delivery invoice, payment, expenditure, statement, return, and rating side effects unchanged. `scripts/sale-project-invoicing-write-http-smoke.ps1`, `scripts/frontend-deferred-write-wrapper-smoke.ps1`, `scripts/frontend-api-method-smoke.ps1`, and `scripts/business-read-http-smoke.ps1` passed against the local backend at `http://127.0.0.1:82`.
- 2026-06-18 team-project task write smoke: added `scripts/team-project-task-write-http-smoke.ps1` as a complete DB-backed team-project task feature smoke. It creates a temporary project and member user, exercises category add/edit/sort/delete, task add/edit/delete, assignee remove/re-add, task-comment add/edit/delete with attachment metadata, validation and not-found guards, project delete, final logical-delete/version/task-user DB back-checks, and cleanup. It passed against the local backend at `http://127.0.0.1:82`.
- 2026-06-18 sale-project foundation closure: replaced `/biz/saleproject/add`, `/edit`, `/history/add`, and `/special/add` controlled-deferred behavior with Java-compatible base project, base edit, historical project, and public reimbursement project maintenance. The block validates customer/user/org/tenant/data-scope access, runs transactions, applies Java-style historical payment correction, and preserves finance/invoice/stock/workflow/file/notification side-effect boundaries; normal add/edit product-list mutation is now covered by the separate product-list mutation block. `scripts/sale-project-foundation-closure-http-smoke.ps1` passed against the local backend, and `scripts/frontend-deferred-write-wrapper-smoke.ps1` now passes with 47 authenticated POST deferred wrappers plus the task-SSE deferred GET wrapper.
- 2026-06-18 feature-closure workflow baseline: changed the active continuation rules from route-by-route safe slices to complete feature-closure blocks. The sale-project foundation block is now complete; choose the next block from the remaining deferred side-effect groups and map Java/frontend/ThinkPHP/database/downstream behavior before code edits. Runtime DB readiness should be judged through `scripts/runtime-ready.ps1`, `scripts/project-preflight.ps1`, or an app-level ThinkPHP DB check, not by the unrelated Windows `MySQL80` service alone.
- 2026-06-18 sale-project deal edit: replaced `/biz/saleproject/deal/edit` controlled-deferred behavior with Java-compatible delivery/freight field maintenance. It validates active sale-project access through the existing tenant/data-scope query, updates only `UNIT`, `ADDRESS`, `LOGISTICS_CATEGORY`, `CONSIGNEE`, `PHONE`, `REMARK`, `FREIGHT`, `FREIGHT_CATEGORY`, and `DELIVERY_NOTE`, and refreshes audit/version fields without touching state, amounts, invoicing, product, finance, or workflow rows. `scripts/sale-project-deal-edit-http-smoke.ps1` was added, but DB-backed execution is pending because local MySQL `MySQL80` is stopped; deferred wrapper smoke should now cover 51 authenticated POST wrappers plus the task-SSE deferred GET wrapper.
- 2026-06-18 sale-project delete: replaced `/biz/saleproject/delete` controlled-deferred behavior with Java-compatible logical delete maintenance. It accepts Java/copied-frontend array payloads, validates every selected active project through the existing tenant/data-scope query, requires `PROJECT_STATE = FOLLOW`, sets selected rows to `DISCARD`, sets `DELETE_FLAG = DELETED`, and refreshes audit/version fields without touching invoicing rows. `scripts/sale-project-delete-http-smoke.ps1` was added, but DB-backed execution is pending because local MySQL `MySQL80` is stopped; deferred wrapper smoke should now cover 52 authenticated POST wrappers plus the task-SSE deferred GET wrapper.
- 2026-06-18 sale-project repeal: replaced `/biz/saleproject/repeal` controlled-deferred behavior with Java-compatible discard maintenance. It accepts Java/copied-frontend array payloads, validates every selected active project through the existing tenant/data-scope query, requires `PROJECT_STATE = FOLLOW`, sets selected rows to `DISCARD`, writes `REPEAL_CONTENT` from the first submitted row/top-level field, and refreshes audit/version fields without touching invoicing rows. `scripts/sale-project-repeal-http-smoke.ps1` was added, but DB-backed execution is pending because local MySQL `MySQL80` is stopped; deferred wrapper smoke should now cover 53 authenticated POST wrappers plus the task-SSE deferred GET wrapper.
- 2026-06-18 sale-project cancel: replaced `/biz/saleproject/cancel` controlled-deferred behavior with Java-compatible status rollback. It validates active sale-project access through the existing tenant/data-scope query, requires `PROJECT_STATE = WAIT_DELIVER`, sets the project back to `FOLLOW`, refreshes audit/version fields, and logically deletes active project invoicing rows for the same tenant. `scripts/sale-project-cancel-http-smoke.ps1` was added, but DB-backed execution is pending because local MySQL `MySQL80` is stopped; deferred wrapper smoke should now cover 54 authenticated POST wrappers plus the task-SSE deferred GET wrapper.
- 2026-06-18 payroll import: replaced `/biz/bizpayroll/import` controlled-deferred behavior with focused Java-template payroll import. It accepts multipart `.xlsx`, parses the existing template layout with PHP ZIP/XML support, reads the salary month from the title row, matches imported names against active users in the requested organization subtree and tenant, inserts one payroll row for each matched row, and returns Java-style partial-success import counts. `scripts/biz-payroll-import-http-smoke.ps1` was added, but DB-backed execution is pending because local MySQL `MySQL80` is stopped; deferred wrapper smoke should now cover 55 authenticated POST wrappers plus the task-SSE deferred GET wrapper.
- 2026-06-18 sale-project amount edit: replaced `/biz/saleproject/amount/edit` controlled-deferred behavior with focused Java-compatible amount maintenance. It validates active sale-project access through the existing tenant/data-scope query, updates `INIT_PRICE`, recalculates collected amount, payment state, project state, total price, refund amount, and return amount, preserves Java's over-collected guard against the pre-recalculation total price, and writes one `INIT_PRICE` field-change log row. `scripts/sale-project-amount-edit-http-smoke.ps1` was added, but DB-backed execution is pending because local MySQL `MySQL80` is stopped; deferred wrapper smoke should now cover 56 authenticated POST wrappers plus the task-SSE deferred GET wrapper.
- 2026-06-18 sale-project visibility edit: replaced `/biz/saleproject/visibility/edit` controlled-deferred behavior with narrow Java-compatible visibility/specimen maintenance. It validates active sale-project access through the existing tenant/data-scope query, accepts `PUBLIC`/`PRIVATE`, requires `specimenCategory` for public visibility, preserves specimen fields for copied frontend private toggles that omit them, and updates only `VISIBILITY`, specimen fields, audit fields, and `VERSION`. `scripts/sale-project-visibility-edit-http-smoke.ps1` was added, but DB-backed execution is pending because local MySQL `MySQL80` is stopped; deferred wrapper smoke should now cover 57 authenticated POST wrappers plus the task-SSE deferred GET wrapper.
- 2026-06-18 dev-job action status: replaced `/dev/job/stopJob`, `/dev/job/runJob`, and `/dev/job/runJobNow` controlled-deferred behavior with narrow Java-compatible `dev_job.JOB_STATUS` transitions. It validates active `{ id }` rows, rejects already-stopped and already-running states like Java, and makes stopped `runJobNow` rows become `RUNNING` without registering scheduler tasks or executing task classes. `scripts/dev-job-write-http-smoke.ps1` was extended, but DB-backed execution is pending because local MySQL `MySQL80` is stopped; deferred wrapper smoke should now cover 58 authenticated POST wrappers plus the task-SSE deferred GET wrapper.
- 2026-06-18 payroll generate add: replaced `/biz/bizpayroll/generate/add` controlled-deferred behavior with Java-compatible payroll generation. It validates selected users under tenant/data-scope guards, reads current-month sale projects, current-month project-payment records, paid sale projects, and leave-of-absence records, applies Java's transaction-volume, received/before-received, vacation overlap, base/payable/actual salary formulas, and inserts one payroll row per selected user in a transaction. Payroll add/import, EasyExcel rendering, workflow hooks, Java data-change events, and duplicate-month prevention remain deferred. `scripts/biz-payroll-generate-add-http-smoke.ps1` was added, but DB-backed execution is pending because local MySQL `MySQL80` is stopped; deferred wrapper smoke should now cover 61 authenticated POST wrappers plus the task-SSE deferred GET wrapper.
- 2026-06-18 purchase-order warehouse batch add: replaced `/biz/bizpurchaseorder/warehouse/add` controlled-deferred behavior with Java-compatible completed-order batch stock-in. It requires `warehousesId`, selects visible completed purchase orders still marked `NOT_IN_WAREHOUSE`, locks the selected orders/items, validates the active warehouse and products, writes `IN` delivery rows with `Process_sys` and `Process_procure_in_warehouse`, creates or increments inventory rows, and marks processed orders/items `IN_WAREHOUSE`; no eligible orders returns `count = 0`. `scripts/purchase-order-warehouse-add-http-smoke.ps1` was added, but DB-backed execution is pending because local MySQL `MySQL80` is stopped on 2026-06-18.
- 2026-06-18 purchase-order warehouse one add: replaced `/biz/bizpurchaseorder/warehouse/one/add` controlled-deferred behavior with Java-compatible single-order stock-in that validates `orderId`, `warehousesId`, write scope, active warehouse, active purchase-order items, and referenced products, writes one `IN` delivery row per item with `Process_sys` and `Process_procure_in_warehouse`, creates or increments inventory rows, and marks the order/items `IN_WAREHOUSE`; batch `/biz/bizpurchaseorder/warehouse/add` is covered separately. At that checkpoint, purchase-order add/delete, workflow, expenditure, settlement-account, and Java data-change event publishing remained deferred; direct purchase-order add/delete was later covered on 2026-06-26. `scripts/purchase-order-warehouse-one-add-http-smoke.ps1` was added, but DB-backed execution is pending because local MySQL `MySQL80` was stopped and failed to start on 2026-06-18.
- 2026-06-18 delivery record add stocktake: replaced `/biz/warehouses/delivery/add` controlled-deferred behavior with Java-compatible system stocktake behavior that validates `warehousesId`, `productId`, target `amount`, and `deliveryTime`, locks the active inventory row, writes one `Process_sys` IN/OUT `delivery_record` row for non-zero movement, updates `inventory.CURRENT_COUNT` to the submitted target, increments `VERSION`, and leaves purchase-order warehouse entry, workflow, finance, and Java data-change event publishing out of scope. `scripts/delivery-record-add-http-smoke.ps1` was added, but DB-backed execution is pending because local MySQL `MySQL80` was stopped and failed to start on 2026-06-18.
- 2026-06-17 purchase-order audit edit: replaced `/biz/bizpurchaseorder/audit/edit` controlled-deferred behavior with narrow Java-compatible audit-remediation editing that validates `id` and unique nonempty `productList`, locks the active order, checks write scope, intentionally skips normal edit's completed-settlement and goods-expenditure guards, requires submitted item ids to belong to the same order, updates only order `AMOUNT` plus existing item amount/cost fields and audit/version fields, and leaves item creation/deletion, warehouse stock-in, inventory, delivery, expenditure, settlement-account, workflow, and Java data-change side effects unchanged; `scripts/purchase-order-audit-edit-http-smoke.ps1` verifies no-token, validation guards, normal-edit rejection on the same guarded order, audit success on a completed goods-expenditure-linked order, detail readback, version/audit update, related-row preservation, and cleanup. Deferred wrapper smoke now covers 65 authenticated POST wrappers plus the task-SSE deferred GET wrapper, and 14 no-token representatives after purchase-order audit edit moved out of the deferred list.
- 2026-06-17 purchase-order edit: replaced `/biz/bizpurchaseorder/edit` controlled-deferred behavior with narrow Java-compatible normal-order editing that validates `id` and unique nonempty `productList`, locks the active order, checks write scope, rejects completed settlement and goods-expenditure-linked orders, requires submitted item ids to belong to the same order, updates only order `AMOUNT` plus existing item amount/cost fields and audit/version fields, and leaves item creation/deletion, audit, warehouse stock-in, inventory, delivery, expenditure, settlement-account, workflow, and Java data-change side effects unchanged; `scripts/purchase-order-edit-http-smoke.ps1` verifies no-token, validation guards, missing/completed/expenditure/wrong-item rollback, detail readback, version/audit update, spoofed field preservation, related-count stability, and cleanup. Deferred wrapper smoke now covers 66 authenticated POST wrappers plus the task-SSE deferred GET wrapper, and 14 no-token representatives after purchase-order edit moved out of the deferred list.
- 2026-06-17 inventory add registration: replaced `/biz/inventory/add` controlled-deferred behavior with narrow warehouse/product inventory registration that validates `warehousesId` and `productIds`, locks the active warehouse, validates active enabled products in the current tenant, rejects duplicates and deleted unique-key conflicts, inserts missing `inventory` rows with `CURRENT_COUNT = 0`, preserves existing active row counts, refreshes audit fields, increments `VERSION`, and leaves stock movement, delivery rows, purchase-order warehouse entry, workflow, and Java data-change events unchanged; `scripts/inventory-add-http-smoke.ps1` verifies no-token, validation guards, missing-product rollback, detail readback, existing-row count preservation/version increment, no delivery-row creation, and cleanup. Deferred wrapper smoke now covers 67 authenticated POST wrappers plus the task-SSE deferred GET wrapper, and 14 no-token representatives after inventory add moved out of the deferred list.
- 2026-06-17 purchase-order cancel: replaced `/biz/bizpurchaseorder/cancel` controlled-deferred behavior with a narrow Java-compatible status marker that validates `id`, locks the active order in the current tenant, checks write scope, rejects completed settlement and in-warehouse orders, updates only `biz_purchase_order.SETTLEMENT_STATUS = Canceled`, audit fields, and `VERSION`, and leaves order items, expenditure rows, inventory, delivery, workflow, and warehouse stock-in state unchanged; `scripts/purchase-order-cancel-http-smoke.ps1` verifies no-token, missing-id, missing-order, completed/in-warehouse guards, detail readback, version/audit update, unchanged related counts, and cleanup. Deferred wrapper smoke now covers 68 authenticated POST wrappers plus the task-SSE deferred GET wrapper, and 14 no-token representatives after purchase-order cancel moved out of the deferred list.
- 2026-06-17 debit-note history add: replaced `/biz/bizdebitnote/history/add` controlled-deferred behavior with a narrow Java-compatible historical debit-note creation path that validates `accountId/amount/historyAmount/createTime/remark`, rejects invalid, missing, and over-settlement input, reads the selected settlement account for tenant/org context, inserts one `biz_debit_note` row with no expenditure link, sets `SETTLEMENT_AMOUNT = HISTORY_AMOUNT`, and leaves settlement-account balances plus payment/expenditure/statement rows unchanged; `scripts/biz-debit-note-history-add-http-smoke.ps1` verifies no-token, failed-case rollback, detail readback, debit-note state, unchanged account/finance counts, and cleanup. Deferred wrapper smoke now covers 69 authenticated POST wrappers plus the task-SSE deferred GET wrapper, and 15 no-token representatives after debit-note history add moved out of the deferred list.
- 2026-06-17 debit-note batch repayment: replaced `/biz/bizdebitnote/batchRepayment/edit` controlled-deferred behavior with a narrow Java-compatible loan-repayment quick-settlement path that validates `accountId/payer/payerTime/items`, rejects duplicate, nonpositive, missing, and over-settlement items, locks selected debit notes, creates `LoanRepayment` `biz_payment_record` and `settlement_account_statement` rows through the settlement-account quick-income path, increments the selected settlement account, updates `biz_debit_note.SETTLEMENT_AMOUNT`, sets `PLAY_STATUS` to `AlreadySettled` when fully settled, and increments `VERSION`; `scripts/biz-debit-note-batch-repayment-http-smoke.ps1` verifies no-token, failed-case rollback, detail readback, statement/payment/account/debit-note state, unchanged unrelated finance counts, and cleanup. Deferred wrapper smoke now covers 70 authenticated POST wrappers plus the task-SSE deferred GET wrapper, and 16 no-token representatives after debit-note batch repayment moved out of the deferred list.
- 2026-06-17 collection-receipt batch expenditure: replaced `/biz/bizcollectionreceipt/batchExpenditure/edit` controlled-deferred behavior with a narrow Java-compatible repayment quick-settlement path that validates `accountId/payer/payerTime/items`, rejects duplicate, nonpositive, missing, and over-settlement items, locks selected collection receipts, creates repayment `biz_expenditure_record` and `settlement_account_statement` rows through the settlement-account quick-expense path, decrements the selected settlement account, updates `biz_collection_receipt.SETTLEMENT_AMOUNT`, sets `PLAY_STATUS` to `AlreadySettled` when fully settled, and increments `VERSION`; `scripts/biz-collection-receipt-batch-expenditure-http-smoke.ps1` verifies no-token, failed-case rollback, detail readback, statement/expenditure/account/receipt state, unchanged unrelated finance counts, and cleanup. Deferred wrapper smoke now covers 71 authenticated POST wrappers plus the task-SSE deferred GET wrapper, and 16 no-token representatives after collection-receipt batch expenditure moved out of the deferred list.
- 2026-06-17 settlement-account transfer add: replaced `/biz/settlementaccount/transfer/add` controlled-deferred behavior with a narrow Java-compatible account-transfer path that validates `expensesAccountId/revenueAccountId/payerTime/amount`, rejects same-account transfers, locks both settlement accounts in stable id order, writes expense-side and income-side `settlement_account_statement` rows with `Process_sys`, writes one linked `biz_expenditure_record` and one linked `biz_payment_record` using fixed `dealings` category, moves the submitted amount between the two account balances, and preserves tenant/user/org links; `scripts/settlement-account-transfer-add-http-smoke.ps1` verifies no-token, missing-field, zero-amount, same-account, missing-account rollback, detail readback, both statements, both finance records, unchanged unrelated finance counts, and cleanup. Deferred wrapper smoke now covers 72 authenticated POST wrappers plus the task-SSE deferred GET wrapper, and 16 no-token representatives after settlement-account transfer add moved out of the deferred list.
- 2026-06-17 settlement-account expenses add: replaced `/biz/settlementaccount/expenses/add` controlled-deferred behavior with a narrow Java-compatible quick-expense path that validates `targetId/settlementCategory/payer/payerTime/amount`, locks the target settlement account, writes one `settlement_account_statement` expense row with `Process_sys`, writes one linked `biz_expenditure_record`, decrements `settlement_account.CURRENT_AMOUNT`, and preserves tenant/user/org links; `scripts/settlement-account-expenses-add-http-smoke.ps1` verified no-token, missing-target, zero-amount, missing-account rollback, detail readback, statement/expenditure/account state, unchanged unrelated finance counts, and cleanup. Deferred wrapper smoke now covers 73 authenticated POST wrappers plus the task-SSE deferred GET wrapper, and 16 no-token representatives after settlement-account expenses add moved out of the deferred list.
- 2026-06-17 settlement-account payment add: replaced `/biz/settlementaccount/payment/add` controlled-deferred behavior with a narrow Java-compatible quick-income path that validates `targetId/settlementCategory/payer/payerTime/amount`, locks the target settlement account, writes one `settlement_account_statement` income row with `Process_sys`, writes one linked `biz_payment_record`, increments `settlement_account.CURRENT_AMOUNT`, and preserves tenant/user/org links; `scripts/settlement-account-payment-add-http-smoke.ps1` verified no-token, missing-target, zero-amount, missing-account rollback, detail readback, statement/payment/account state, unchanged unrelated finance counts, and cleanup. Deferred wrapper smoke now covers 74 authenticated POST wrappers plus the task-SSE deferred GET wrapper, and 16 no-token representatives after settlement-account payment add moved out of the deferred list.
- 2026-06-17 expenditure-record account switch: replaced `/biz/bizexpenditurerecord/edit/account` controlled-deferred behavior with a narrow authenticated account switch that validates `id/currentTargetId/targetId`, verifies the expenditure record and linked statement are still on the current account, locks both settlement accounts in one transaction, adds the stored expenditure amount back to the current account, subtracts it from the target account, updates `biz_expenditure_record.TARGET_ID`, and syncs `settlement_account_statement.ACCOUNT_ID`; `scripts/biz-expenditure-record-edit-account-http-smoke.ps1` verified no-token, missing-target, same-account, current-account mismatch, missing-statement rollback, detail readback, stored-amount balance movement, statement account sync, preserved org, ignored spoofed fields, unchanged related-table counts, and cleanup. Deferred wrapper smoke now covers 74 authenticated wrappers and 17 no-token representatives after expenditure-record account switch moved out of the deferred list.
- 2026-06-17 payment-record account switch: replaced `/biz/bizpaymentrecord/edit/account` controlled-deferred behavior with a narrow authenticated account switch that validates `id/currentTargetId/targetId`, verifies the payment record and linked statement are still on the current account, locks both settlement accounts in one transaction, subtracts the stored payment amount from the current account, adds it to the target account, updates `biz_payment_record.TARGET_ID`/`ORG`, and syncs `settlement_account_statement.ACCOUNT_ID`; `scripts/biz-payment-record-edit-account-http-smoke.ps1` verified no-token, missing-target, same-account, current-account mismatch, missing-statement rollback, detail readback, stored-amount balance movement, statement account sync, ignored spoofed fields, unchanged related-table counts, and cleanup. Deferred wrapper smoke now covers 75 authenticated wrappers and 17 no-token representatives after payment-record account switch moved out of the deferred list.
- 2026-06-16 expenditure-record correction: replaced `/biz/bizexpenditurerecord/edit` controlled-deferred behavior with a narrow authenticated edit that updates `biz_expenditure_record.PAYER_TIME` and/or `SETTLEMENT_CATEGORY`, syncs the linked `settlement_account_statement.PAYER_TIME` when supplied, rejects object-linked records and protected category changes, and preserves object/target/serial/process/amount/account/tenant/delete fields; `scripts/biz-expenditure-record-edit-http-smoke.ps1` verified no-token and missing-field rejection, detail readback, linked-statement sync, category guards, object guard, missing-statement rollback, unchanged finance row counts, and cleanup. Deferred wrapper smoke now covers 76 authenticated wrappers and 17 no-token representatives after expenditure-record edit moved out of the deferred list.
- 2026-06-16 payment-record payer-time correction: replaced `/biz/bizpaymentrecord/edit` controlled-deferred behavior with a narrow authenticated edit that updates only `biz_payment_record.PAYER_TIME` and the linked `settlement_account_statement.PAYER_TIME`; `scripts/biz-payment-record-edit-http-smoke.ps1` verified no-token and missing-field rejection, detail readback, linked-statement sync, ignored client-spoofed fields, missing-statement rollback, unchanged finance row counts, and cleanup. Deferred wrapper smoke now covers 77 authenticated wrappers and 17 no-token representatives after payment-record edit moved out of the deferred list.
- 2026-06-16 payroll CSV export download: replaced `/biz/bizpayroll/export` controlled-deferred wrapper with an authenticated CSV blob download that reuses payroll filters/data-scope and organization sorting; `scripts/biz-payroll-export-http-smoke.ps1` inserted a temporary payroll row, verified CSV headers and row marker through HTTP, checked no-token `code=401`, confirmed representative related table counts stayed unchanged, and cleaned the temporary row. Deferred wrapper smoke now covers 78 authenticated wrappers and 17 no-token representatives after payroll export moved out of the deferred list.
- 2026-06-16 dev-config editBatch value maintenance: replaced `/dev/config/editBatch` controlled-deferred wrapper with narrow existing-row `dev_config.CONFIG_VALUE` batch maintenance; `scripts/dev-config-edit-batch-http-smoke.ps1` verified no-token, empty-list, and missing-value rejection, multi-row batch update across `SYS_BASE`, provider-style, and `BIZ_DEFINE` categories, sensitive detail masking, `******` raw-value preservation, duplicate-key rejection, mixed missing-key rollback, and temporary-row cleanup. Deferred wrapper smoke now covers 79 authenticated wrappers and 18 no-token representatives after dev-config editBatch moved out of the deferred list.
- 2026-06-16 CC-record current-user maintenance: replaced `/biz/ccrecords/add` and `/edit` controlled-deferred wrappers with narrow `biz_cc_records` row maintenance scoped to the current token user; `scripts/biz-cc-records-write-http-smoke.ps1` verified no-token and missing-field rejection, add/detail readback, client-spoofed user/deleteFlag ignored, edit field whitelist, failed mixed delete rollback, logical delete hiding, unchanged file-relation/task counts, and temporary-row cleanup. Deferred wrapper smoke now covers 80 authenticated wrappers and 19 no-token representatives after CC add/edit moved out of the deferred list.
- 2026-06-16 tenant metadata maintenance: replaced `/tenants/tenant/add`, `/edit`, and `/delete` controlled-deferred wrappers with narrow `tenants` row metadata maintenance; `scripts/tenant-write-http-smoke.ps1` verified no-token rejection, missing-name and duplicate guards, add/detail readback, system-tenant edit/delete protection, safe-password delete marker, failed mixed delete rollback, logical delete hiding, default sys user/role/resource/relation counts unchanged, and temporary-row cleanup. Deferred wrapper smoke now covers 82 authenticated wrappers and 20 no-token representatives after tenant add/edit/delete moved out of the deferred list.
- 2026-06-16 gen-config edit/delete metadata maintenance: replaced `/gen/config/edit` and `/delete` controlled-deferred wrappers with narrow field-config metadata maintenance while keeping `/gen/config/add` deferred because Java `GenConfigController` has no add route; `scripts/gen-config-write-http-smoke.ps1` verified no-token rejection, add deferred response, missing-field and missing-id guards, edit readback with boolean normalization and audit/delete field preservation, failed mixed delete rollback, logical delete hiding, parent `gen_basic` preservation, and temporary-row cleanup. Deferred wrapper smoke now covers 85 authenticated wrappers and 21 no-token representatives after gen-config edit/delete moved out of the deferred list.
- 2026-06-16 gen-basic metadata maintenance: replaced `/gen/basic/add`, `/edit`, and `/delete` controlled-deferred wrappers with narrow generator metadata maintenance; `scripts/gen-basic-write-http-smoke.ps1` verified no-token rejection, add/detail/config readback, generated default config rows, invalid table/key guards, same-table primary-key refresh, table-change config rebuild, failed mixed delete rollback, logical delete of basic/config rows, and temporary-row cleanup. Deferred wrapper smoke now covers 89 authenticated wrappers and 21 no-token representatives after gen-basic add/edit/delete moved out of the deferred list.
- 2026-06-16 dev-job metadata maintenance: replaced `/dev/job/add` and `/dev/job/edit` controlled-deferred wrappers with narrow metadata maintenance; `scripts/dev-job-write-http-smoke.ps1` verified no-token rejection, add/detail readback, generated code/default stopped status, duplicate rejection, invalid category/cron/actionClass guards, edit preservation of code/status, running-job edit rejection, and temporary-row cleanup. Deferred wrapper smoke then covered 92 authenticated wrappers and 21 no-token representatives after dev-job add/edit moved out of the deferred list.
- 2026-06-16 biz user vacation manual maintenance: replaced the vacation add/edit/delete controlled-deferred wrappers with narrow transactional manual maintenance; added `scripts/biz-user-vacation-write-http-smoke.ps1`, verified no-token rejection, add/detail readback, duplicate rejection, edit version increment, invalid amount guard, mixed delete rollback guard, logical delete hiding, and unchanged leave/payroll row counts. Deferred wrapper smoke now covers 94 authenticated wrappers and 21 no-token representatives after vacation was removed from the deferred list.
- 2026-06-16 guarded browser smoke refresh: reran `scripts/browser-upload-provider-guard-smoke.ps1` across the default upload/provider set plus management, finance/purchase/inventory, sales/operations/report, workflow, home, HR, history, team-project list, and member-visible team-project detail targets; all 53 guarded targets passed with zero forbidden requests and zero bad API statuses.
- 2026-06-16 project-preflight coverage hardening: `scripts/project-preflight.ps1` now includes frontend API route-gap smoke and frontend deferred write wrapper smoke by default, with `-SkipFrontendApiRouteGap` and `-SkipFrontendDeferredWrites` escape hatches. A full default preflight run reached the later dev-read phase before the tool-level 300s timeout; all remaining dev/gen/auth/directory/tenant/message-SSE/workflow smokes were rerun individually and passed.
- 2026-06-16 workflow/task/sale-project controlled-deferred wrappers: added protected `code=400` wrappers for process start/edit/cancel, task approve/reject/SSE stream, and sale-project add/edit/delete/state/amount/deal/cancel/history/special/visibility/repeal actions; PHP lint, route-list checks, `scripts/frontend-deferred-write-wrapper-smoke.ps1`, frontend API method smoke, and route-gap scan passed. Static frontend route-path coverage is now 560 of 560 endpoints with zero missing paths.
- 2026-06-16 dev/gen/tenant controlled-deferred wrappers: initially added protected `code=400` wrappers for dev config editBatch, email sends, job add/edit/run/stop, generator basic/config writes, direct generator project execution, and tenant add/edit/delete; later dev-config, dev-job, gen-basic, gen-config, and tenant metadata slices replaced dev config editBatch, job add/edit, gen-basic add/edit/delete, gen-config edit/delete, and tenant add/edit/delete, while provider sends, job run/stop/run-now, gen config add, direct project generation, and tenant bootstrap remain deferred. PHP lint, route-list checks, `scripts/frontend-deferred-write-wrapper-smoke.ps1`, frontend API method smoke, and route-gap scan passed.
- 2026-06-16 HR/CC controlled-deferred wrappers: initially added protected `code=400` wrappers for leave add, payroll add/import/export/generate, vacation add/edit/delete, and CC add/edit entry points; later vacation and CC maintenance slices replaced vacation add/edit/delete and CC add/edit, payroll export, generate, and import moved to focused implementations, while leave add, payroll add, and workflow copy generation remain deferred. PHP lint, route-list checks, `scripts/frontend-deferred-write-wrapper-smoke.ps1`, frontend API method smoke, and route-gap scan passed.
- 2026-06-16 inventory/delivery/settlement controlled-deferred wrappers: added protected `code=400` wrappers for inventory add/delete, delivery add, and settlement-account delete/expenses/payment/transfer entry points; PHP lint, route-list checks, `scripts/frontend-deferred-write-wrapper-smoke.ps1`, frontend API method smoke, and route-gap scan passed. Route-path coverage is now 500 frontend endpoints with zero read-like gaps and 60 side-effect-like deferred gaps.
- 2026-06-16 purchase-order controlled-deferred wrappers: added protected `code=400` wrappers for purchase-order add/edit/audit/cancel/delete and warehouse stock-in entry points; PHP lint, route-list checks, `scripts/frontend-deferred-write-wrapper-smoke.ps1`, frontend API method smoke, and route-gap scan passed. Route-path coverage is now 493 frontend endpoints with zero read-like gaps and 67 side-effect-like deferred gaps.
- 2026-06-16 collection-receipt/debit-note controlled-deferred wrappers: added protected `code=400` wrappers for collection-receipt add/edit/delete/batchExpenditure and debit-note add/edit/delete/batchRepayment/history add; PHP lint, route-list checks, `scripts/frontend-deferred-write-wrapper-smoke.ps1`, frontend API method smoke, and route-gap scan passed. Route-path coverage is now 486 frontend endpoints with zero read-like gaps and 74 side-effect-like deferred gaps.
- 2026-06-16 expenditure-record controlled-deferred wrappers: added protected `code=400` wrappers for `/biz/bizexpenditurerecord/add`, `/edit`, `/edit/account`, and `/delete`; `scripts/frontend-deferred-write-wrapper-smoke.ps1`, PHP lint, route-list checks, frontend API method smoke, and route-gap scan passed. Route-path coverage is now 477 frontend endpoints with zero read-like gaps and 83 side-effect-like deferred gaps.
- 2026-06-16 frontend API route-gap scanner: added `scripts/frontend-api-route-gap-smoke.ps1` plus docs to compare copied frontend wrapper request paths with `php think route:list`; summary mode and `-FailOnReadMissing` pass with 560 unique frontend endpoints, 473 route-path-covered endpoints, zero read-like missing routes, and 87 side-effect-like deferred gaps.
- 2026-06-16 full project preflight after guarded browser smoke refresh: `scripts/project-preflight.ps1` passed Git status, runtime readiness, web readiness, frontend API method smoke, role/user/business/inventory/finance/purchase/settlement/supplier/product/HR/team-project/datareport/resource/dev/gen/auth/directory/tenant/message-SSE/workflow HTTP smokes, and Git whitespace checks; only normal LF/CRLF Git warnings were emitted.
- 2026-06-16 workflow/home/HR/team guarded browser smoke refresh: reran the upload/provider guard helper against workflow task/process pages, `/index`, leave/history/team-project list pages, and `/biz/bizteamprojectdetails?id=<member-visible local sample>`; all passed with zero forbidden requests and zero bad API statuses. `scripts/browser-page-smoke.ps1` now expands object-shaped console errors, which exposed an intentionally inaccessible arbitrary team-project sample before the smoke was rerun with a project visible to the current smoke account.
- 2026-06-16 sales/operations/report guarded browser smoke batch: after restarting the local MySQL/Redis runtime and waiting for Vite cold start, ran the upload/provider guard helper against sale-project list/report pages, operations customer/project pages, proxy-payment, security-deposit, and four data-report pages; all passed list/report-only with zero forbidden requests and zero bad API statuses.
- 2026-06-16 finance/purchase/inventory guarded browser smoke batch: ran the upload/provider guard helper against purchase-order, return-order, collection-receipt, debit-note, inventory, payment-record, expenditure-record, settlement-account, warehouse, supplier, invoicing, and sale-project-product-info pages; all passed list-only with read/cache/SSE requests and no export, delete, upload, settlement, stock, provider, workflow, or business-write requests. Raw resource/mobile/gen routes were skipped because they are not in the current `loginMenu` without temporary authorization.
- 2026-06-16 management-page guarded browser smoke batch: reran the upload/provider guard helper against `/sys/user`, `/biz/user`, `/sys/role`, `/sys/org`, `/sys/position`, `/biz/org`, `/biz/position`, `/biz/dict/index`, `/sys/sysConfig/index`, and `/dev/message/index`; all rendered with read/cache/SSE requests only and no import, export, grant, reset, send, save, upload, delete, or provider requests.
- 2026-06-16 upload/provider guarded browser smoke batch: added `scripts/browser-upload-provider-guard-smoke.ps1` and verified `/dev/file/index`, `/biz/bizpayroll`, `/biz/bizproduct`, `/biz/customer`, and `/biz/saleproject/dealProjectList` with a stricter forbidden pattern; no upload, import, export, delete, send, provider, or business-write requests were triggered.
- 2026-06-16 frontend controlled-deferred write wrappers: added protected `code=400` wrappers and copied-frontend API exports for payment-record, return-order, and sale-project-invoicing write controls; `scripts/frontend-deferred-write-wrapper-smoke.ps1`, `scripts/frontend-api-method-smoke.ps1 -ShowDeferred`, and targeted `php think route:list` checks passed.
- 2026-06-16 frontend API static smoke comment handling: `scripts/frontend-api-method-smoke.ps1` now strips Vue/HTML comments and JavaScript line/block comments before scanning imports and API calls; rerun passed and `-ShowDeferred` no longer reports the commented legacy `bizSaleProjectApplyApproval` call.
- 2026-06-16 upload/provider deferred execution plan: added `docs/tasks/upload-provider-deferred-plan.md` and linked it from the API gap map so future browser smoke for upload-control pages has explicit forbidden-request patterns and cloud/provider/file-cleanup stop conditions before any implementation.
- 2026-06-16 browser smoke helper optional-click mode: `scripts/browser-page-smoke.ps1` now supports `-AllowMissingTableLink` with `-ClickFirstTableLink`, preserving strict click failures by default while allowing empty/local-sample task pages to pass with `click.missingAllowed=true`; verified against `/biz/biztask` and `/biz/biztask/mystarttask`.
- 2026-06-16 home/team-project detail browser smoke: login-menu-backed `/index` and `/biz/bizteamprojectdetails?id=<local sample>` rendered with read-only API traffic; the team-project detail route used a real local project id and avoided comment, member, task, edit, delete, and upload controls. Current login-menu component leaf pages are now covered by read-only browser smoke, excluding category/grouping nodes without page components.
- 2026-06-16 workflow menu browser smoke: login-menu-backed `/biz/biztask`, `/biz/historytask`, `/biz/biztask/mystarttask`, `/biz/biztask/allprocess`, `/biz/copytask`, and `/biz/biztask/processList` rendered with read-only API traffic; `/biz/biztask/mystarttask` opened a process detail drawer, while empty/local-sample task lists and start-flow buttons stayed list/static-only.
- 2026-06-16 operations/invoicing/debit browser smoke: login-menu-backed `/biz/bizops/operationCustomerList`, `/biz/bizops/operationProjectList`, `/biz/saleprojectinvoicing`, `/biz/saleprojectproductinfo`, `/biz/proxyPayment`, and `/biz/ProjectSecurityDeposit` rendered with read-only API traffic; operations pages opened safe customer/project detail drawers, while invoicing, product-info, proxy-payment, and deposit pages stayed list/report-only.
- 2026-06-16 sales-project menu browser smoke: login-menu-backed `/biz/saleproject`, `/biz/saleproject/public/list`, `/biz/saleproject/dealProjectCaseList`, `/biz/saleproject/waitShipment`, `/biz/saleproject/completeProjectList`, `/biz/saleproject/cancelProjectList`, and `/biz/saleproject/report` rendered with read-only API traffic; main/public/case pages opened safe detail drawers, while shipment, completed, cancelled, and report pages stayed list/chart-only.
- 2026-06-16 business directory browser smoke: login-menu-backed `/biz/org`, `/biz/position`, `/biz/user`, and `/biz/dict/index` rendered their tree/list pages with read-only API traffic; no forbidden write, upload, delete, import, export, grant, reset, save, or provider requests were triggered.
- 2026-06-16 system directory browser smoke: login-menu-backed `/sys/org` and `/sys/position` rendered their organization tree plus list pages with read-only API traffic; no forbidden write, upload, delete, grant, reset, status, save, or provider requests were triggered.
- 2026-06-16 dev and system browser smoke: login-menu-backed paths `/dev/file/index`, `/dev/message/index`, `/sys/sysConfig/index`, `/sys/role`, and `/sys/user` rendered with only read/cache requests; a business-route regression check on `/biz/customer` still passed after the helper started selecting the target route's menu module.
- 2026-06-16 data-report and team-project browser smoke: menu-backed report paths `/biz/bizdatareport/index`, `/summaryStatistics`, `/settlement`, and `/saleProfit` rendered and completed their read APIs; `/biz/bizteamproject` rendered the card-list page; no forbidden write, upload, delete, approval, complete, export, task/comment, or provider requests were triggered.
- 2026-06-16 HR and history browser smoke: `/biz/bizpayroll` rendered 2 rows as a list-only smoke after filtering the known Ant Design Vue resizable-table warning; `/biz/bizleaveapplication` rendered 1 row list-only; `/biz/bizhistoryexcel` rendered 1 row list-only because no visible safe detail link was available in the local sample; no forbidden write, upload, delete, approval, complete, export, import, or provider requests were triggered.
- 2026-06-16 purchase/return/receivable/debit browser smoke: `/biz/bizpurchaseorder` rendered 10 rows and opened purchase-order detail; `/biz/returnorder` rendered 1 row and opened sale-project detail; `/biz/bizcollectionreceipt` and `/biz/bizdebitnote` each rendered 10 rows as list-only smokes; no forbidden write, upload, delete, approval, complete, export, settlement-mark, warehouse-entry, or provider requests were triggered.
- 2026-06-16 inventory and finance browser smoke: `/biz/inventory` rendered 10 rows and opened product detail; `/biz/paymentrecord` and `/biz/bizexpenditurerecord` each rendered 10 rows as list-only finance smokes; no forbidden write, upload, delete, approval, complete, export, or provider requests were triggered.
- 2026-06-16 settlement-account and warehouse browser smoke: `/biz/settlementaccount` rendered 10 rows and opened account detail with payment/expenditure list reads; `/biz/warehouses` rendered 4 rows as a list-only smoke because its table links are edit/delete actions; both had no forbidden write, upload, delete, approval, complete, or provider requests.
- 2026-06-16 product and supplier browser smoke: `/biz/bizproduct` rendered 10 rows and opened product detail; `/biz/supplier` rendered 11 rows and opened supplier detail with related purchase-order list read; both had no forbidden write, upload, delete, approval, complete, or provider requests.
- 2026-06-16 customer browser smoke: `/biz/customer` rendered 11 rows, opened the first customer detail drawer, observed customer page/detail plus read-only local file download for the business-license image, and had no forbidden write, upload, delete, approval, complete, or provider requests.
- 2026-06-16 reusable browser smoke helper: added `scripts/browser-page-smoke.ps1` and reran `/biz/saleproject/dealProjectList`; it rendered 5 rows, opened the first project detail drawer, and observed only read/SSE backend requests with no forbidden write, upload, delete, approval, complete, or provider requests.
- 2026-06-16 sale-project dealProjectList browser smoke: `/biz/saleproject/dealProjectList` rendered 5 rows, opened the first project detail drawer, and observed only read/SSE backend requests with no forbidden write, upload, delete, approval, complete, or provider requests.
- 2026-06-15 workflow read-only compatibility: `/biz/task/page`, `/biz/task/history/page`, `/biz/process/page`, `/biz/process/all/page`, and `/biz/ccrecords/page` returned HTTP 200 with `code=200` against the local runtime.
- 2026-06-15 workflow query-list guard: `/biz/process/query/list` now accepts Java-style JSON body filters and rejects empty filters with `400`; full `project-preflight` passed.
- 2026-06-15 workflow detail browser smoke: `/biz/biztask/mystarttask` rendered 3 rows, opened a process detail drawer, called process detail/fileList/variable reads, and observed no console errors or forbidden write/upload/delete requests.
- 2026-06-15 workflow browser smoke: copied Vue routes `/biz/biztask`, `/biz/biztask/historyTask`, `/biz/biztask/mystarttask`, `/biz/biztask/allprocess`, and `/biz/biztask/copytask` rendered tables or empty states through the frontend dev server with no blocking console errors and no observed workflow write requests.
- Workflow task rows now preserve task ids in `id`/`taskId`; process instance ids are exposed through `instanceId` and `processInstanceId`. Process rows continue to use the process instance id as `id`.
- 2026-06-15 public auth/user-center wrapper compatibility: `/auth/b/getPhoneValidCode`, `/auth/b/subscription`, `/sys/userCenter/findPasswordGetPhoneValidCode`, `/sys/userCenter/findPasswordGetEmailValidCode`, `/sys/userCenter/findPasswordByPhone`, and `/sys/userCenter/findPasswordByEmail` are routed and return controlled `code=400` deferred responses without SMS/email/WebPush/password mutations.
- 2026-06-15 third-party auth wrapper compatibility: `/auth/third/render` and `/auth/third/callback` are routed and return controlled `code=400` deferred responses without OAuth redirects, callback token issuance, user binding writes, provider config reads, or database changes.
- 2026-06-15 SMS provider wrapper compatibility: `/dev/sms/sendAliyun`, `/dev/sms/sendTencent`, and `/dev/sms/sendXiaonuo` are routed behind auth and return controlled `code=400` deferred responses without provider credential reads, SDK calls, external sends, send-record inserts, or database changes.
- 2026-06-15 user display HTTP smoke: `scripts/user-display-http-smoke.ps1` verified `/sys/user/page`, `/biz/user/page`, `/sys/user/detail`, `/sys/user/list/detail`, `/sys/user/userSelector`, and `/biz/user/userSelector` expose frontend-visible `orgName`, `positionName`, `genderName`, selector aliases, Java-style paging keys, and no `PASSWORD`.
- 2026-06-15 business read HTTP smoke: `scripts/business-read-http-smoke.ps1` verified authenticated customer page/detail/detail-list reads, sale-project page/case/operation/public/detail/list-detail/product/cost/cost-details reads, and return-order page/query/detail reads without calling customer writes, return-order writes, sale-project state writes, workflow, finance, stock, or provider actions.
- 2026-06-15 follow-up read smoke extension: `scripts/business-read-http-smoke.ps1` now also verifies customer follow-up and sale-project follow-up page/detail read payloads when local sample data exists, without calling follow-up writes, attachment cleanup, notifications, workflow, finance, or inventory.
- 2026-06-15 sale-project billing nested read smoke extension: `scripts/business-read-http-smoke.ps1` now verifies invoicing page/detail/customer reads, invoice page/list nested `invoiceItems`, invoice-item page filtering, and reissue-order nested `productItemList`, without calling invoicing complete, stock, settlement, finance, workflow, or file cleanup.
- 2026-06-15 sale-project product/package relation read smoke extension: `scripts/business-read-http-smoke.ps1` now verifies product-info page/list/detail and product-item relation list payloads, without calling product-info writes, relation mark edits, product-item mark edits, inventory, delivery, finance, workflow, or file cleanup.
- 2026-06-15 inventory/delivery read HTTP smoke: `scripts/inventory-delivery-read-http-smoke.ps1` verifies inventory page/list/detail and warehouse delivery page/detail/export-list reads without calling stock movement, delivery writes, inventory mutations, finance, workflow, provider, or data-change behavior.
- 2026-06-15 finance read HTTP smoke: `scripts/finance-read-http-smoke.ps1` verifies payment-record, expenditure-record, collection-receipt, and debit-note page/list/detail reads without calling mark-success, account transfers, statements, repayment, expenditure generation, workflow, provider, or finance mutations.
- 2026-06-15 purchase-order read HTTP smoke: `scripts/purchase-order-read-http-smoke.ps1` verifies purchase-order page/list/detail-list/detail wrapper reads without calling add/edit/audit/delete/cancel, warehouse stock movement, expenditure mutation, workflow, provider, or data-change behavior.
- 2026-06-15 settlement-account payment read HTTP smoke: `scripts/settlement-account-payment-read-http-smoke.ps1` verifies settlement-account statement page/list reads without calling settlement payment creation, expenses/income actions, transfer actions, balance mutation, workflow, provider, or data-change behavior.
- 2026-06-15 settlement-account read HTTP smoke: `scripts/settlement-account-read-http-smoke.ps1` verifies settlement-account page/list/detail/queryName reads without calling add/edit/status/delete, expenses/payment/transfer actions, balance mutation, workflow, provider, or data-change behavior.
- 2026-06-15 supplier/warehouse read HTTP smoke: `scripts/supplier-warehouse-read-http-smoke.ps1` verifies supplier page/list/query-name/detail and warehouse page/list/detail reads without calling master-data writes, inventory, purchase, payment, workflow, provider, or data-change behavior.
- 2026-06-15 product read HTTP smoke: `scripts/product-read-http-smoke.ps1` verifies product page/list/detail and read-only kit children lookup without calling product writes, status/reconciliation edits, inventory, purchase, finance, workflow, provider, file upload, or data-change behavior.
- 2026-06-15 HR read HTTP smoke: `scripts/hr-read-http-smoke.ps1` verifies payroll page/mypage/detail, leave-application page/my-page/detail, user-vacation page/detail, and history-Excel page/detail reads without calling payroll edit/batch/delete/template download, leave writes, vacation manual maintenance, Excel writes/import/export, workflow, payroll recalculation, provider, or data-change behavior.
- 2026-06-15 team-project read HTTP smoke: `scripts/team-project-read-http-smoke.ps1` verifies team-project page/detail, member page/list/detail, task-category page/list/detail, task page/list/detail, task-user page/detail, project-comment page/list/detail, comment-reply page/detail, and task-comment page/list/detail reads without calling project, member, category, task, assignee, comment, reply, notification, realtime, or data-change writes.
- 2026-06-15 datareport read HTTP smoke: `scripts/datareport-read-http-smoke.ps1` verifies sale-project amount/list/details/report/unpaid, settlement income/expenses, sale-profit, and summary-statistics report reads; it also fixed `bizdatareport` specific route ordering and JSON body parsing for copied POST report filters, without calling settlement/payment/transfer/mark-success/workflow/provider/data-change writes.
- 2026-06-15 resource read HTTP smoke: `scripts/resource-read-http-smoke.ps1` verifies system module/menu/button/field page/tree/detail/selectors and mobile module/menu/button tree/detail/selectors without calling resource add/edit/delete, menu change-module, role grants, cache invalidation, data-change events, or mobile role mutations.
- 2026-06-15 dev read HTTP smoke: `scripts/dev-read-http-smoke.ps1` verifies dev config/dict/file/job/log/monitor/message page-style reads and log chart reads without calling uploads, downloads, deletes, job execution, config writes, message detail mark-read, message send/delete, Email/SMS/provider routes, or external services.
- 2026-06-15 gen read HTTP smoke: `scripts/gen-read-http-smoke.ps1` verifies generator basic page/detail/tables/tableColumns/mobileModuleSelector/config list/detail/preview reads without calling generator add/edit/delete, config editBatch, ZIP download, direct project generation, template execution, or source/schema writes.
- 2026-06-15 auth/index read HTTP smoke: `scripts/auth-index-read-http-smoke.ps1` verifies login-user, session monitor analysis/page reads, third-party binding page, user-center login menu/org/position/workbench/process-config/message page reads, and homepage schedule/message/log lists without calling session/token exits, password recovery, user-center writes, message detail mark-read, all-message mark-read, or schedule writes.
- 2026-06-15 directory alias HTTP smoke: `scripts/directory-alias-http-smoke.ps1` verified system org selector plus business org/position/dict alias pages, trees, and selectors, including org selector `name` aliases and `current=1&size=1` pagination compatibility; `OrgService::pagination()` now accepts `size`.
- 2026-06-15 workflow read HTTP smoke: `scripts/workflow-read-http-smoke.ps1` verifies task count/list/page/history, process page/all-page/query/query-list/detail/variable/file-list, project runtime query when sample data exists, and CC page/detail when current-user sample data exists, without calling approve/reject/start/cancel/delete/SSE.
- 2026-06-15 selector pagination compatibility: authenticated HTTP smoke verified Java-style `records/current/total` payloads for system and business user/position selectors used by copied `XnPageSelect` and `XnUserSelector`; role selector services now return the same paged shape for copied `roleSelectorPlus`.
- 2026-06-15 continuation speed helpers: `scripts/project-progress.ps1` now summarizes progress/problem/context handoff state and can run both runtime readiness and web readiness checks before DB, HTTP, or browser smokes.
- 2026-06-15 role selector HTTP smoke: `scripts/role-selector-http-smoke.ps1` verifies the authenticated read-only role selector payloads used by copied user grant dialogs without requiring local Playwright/Puppeteer.
- 2026-06-15 tenant read HTTP smoke: `scripts/tenant-read-http-smoke.ps1` verifies `/tenants/tenant/page` and `/tenants/tenant/detail` authenticated read shape; tenant add/edit/delete now have focused write smoke coverage, while default-data generation remains deferred.
- 2026-06-15 frontend API method smoke: `scripts/frontend-api-method-smoke.ps1` verifies copied Vue views/components do not call missing read-like API methods that would crash before reaching backend routes.
- 2026-06-15 message SSE HTTP smoke: `scripts/message-sse-http-smoke.ps1` verifies `/dev/message/createSseConnect` returns `text/event-stream`, echoes the client id, and emits `FlushMessageNotice` plus `FlushProcessNotice`.
- 2026-06-15 local preflight bundle: `scripts/project-preflight.ps1` runs Git status, runtime readiness, web readiness, frontend API method smoke, frontend route-gap smoke, frontend deferred write wrapper smoke, role-selector HTTP smoke, user-display HTTP smoke, business-read HTTP smoke, inventory/delivery-read HTTP smoke, finance-read HTTP smoke, purchase-order-read HTTP smoke, settlement-account-payment-read HTTP smoke, settlement-account-read HTTP smoke, supplier/warehouse-read HTTP smoke, product-read HTTP smoke, HR-read HTTP smoke, team-project-read HTTP smoke, datareport-read HTTP smoke, resource-read HTTP smoke, dev-read HTTP smoke, gen-read HTTP smoke, auth/index-read HTTP smoke, directory-alias HTTP smoke, tenant-read HTTP smoke, message-SSE HTTP smoke, workflow-read HTTP smoke, and whitespace checks as one command with skip switches.

## Module Progress

| Area | Completion | Status | Done | Remaining |
| --- | ---: | --- | --- | --- |
| Project engineering setup | 95% | Green | Git, remote sync, worktree plan, docs, AGENTS rules, baseline commands | Keep docs updated after each slice |
| Database and Models | 85% | Green | 121-table SQL reference used; 67 passive Models; field/relation/index docs | Low-priority tables, model relation methods where needed, final migration review |
| Auth / Token / RBAC / Menu | 78% | Yellow | Login, password compatibility base, token middleware, token cache index, RBAC/menu/session reads, role add/edit/delete, role resource/mobile-menu/permission/user grant saves, session/token exit compatibility, public phone-code/WebPush wrapper paths with controlled deferred responses, third-party binding page read, protected routes | Refresh/session hardening, fine-grained permission enforcement, data-scope expansion, C-side client auth, real phone-code login/SMS, WebPush subscription persistence, third-party OAuth render/callback deferred |
| User / Org / Position | 98% | Green | Organization tree reads plus system/business org add/edit/delete, position reads plus system/business position add/edit/delete, user directory, paged selector payloads for copied user/position/role selector components, user-center selectors, public password-recovery captcha plus controlled deferred verification/reset wrapper paths, business-side directory aliases, camelCase display aliases for org/user/position pages, system/business user add/edit/delete, system user import, user role/resource/permission grant saves, user enable/disable, admin reset password, user export/download blobs, current-user profile/password/avatar/signature/workbench/process-config writes, user/index message detail mark-read, homepage all-message mark-read, and homepage schedule add/delete | Business user import is intentionally absent like Java; real password-recovery SMS/email/reset, real `.docx` rendering, and encrypted profile fields remain |
| Workflow | 80% | Yellow | Runtime strategy, read-only task/process routes, process query aliases, file-list reads, runtime activity detail, variable normalization, copy/CC record reads, annual-leave balance page/detail reads, minimal leave start with active copy-user CC generation and workflow file binding, active leave cancel/edit, minimal leave approve/reject, approved leave-row generation, approved `annualLeave` balance deduction, workflow-approved `leaveOfAbsence` payroll-generation coverage, direct leave-row annual-leave balance adjustment, five non-project first-step starts/cancel, `Process_payment` approval payment-in settlement, `Process_reimbursement`/`Process_make_payment` two-step payment-out settlement, `Process_procure` approval purchase-order creation, `Process_procure_in_warehouse` approval stock-in, `Process_sale_project_init` start/approval/cancel/reject side effects, `Process_sale_project_delivery` start/approval/cancel/reject stock side effects, `Process_project_reissue_product` start/approval/cancel/reject reissue-order side effects, `Process_sale_project_product_return` start/approval/cancel/reject return-order/IN-delivery/inventory/account-backed-auto-refund side effects, direct return-order reverse stock/finance correction, `ReturnAndRefund` expense correction, and `Process_sale_project_play` start/approval/cancel/reject/two-step finance side effects | Non-leave edit; task SSE; automatic existing-payroll row updates; broader workflow write runtime. No-account return auto-refund was reviewed and is not a Java-parity target unless requested as new product behavior |
| Dev/System/Mobile/Gen/Tenant reads | 88% | Yellow | Many management endpoints added and routed, including sys process-config detail/edit, dev config `BIZ_DEFINE` add/edit/delete and editBatch value maintenance, dev log category delete, dev job metadata add/edit/delete, gen basic metadata add/edit/delete, gen config edit/delete/editBatch metadata saves, gen basic metadata reads plus safe preview buckets and ZIP download, LOCAL/dynamic file upload, dev-file metadata logical delete, email/SMS metadata logical delete, protected SMS provider-send deferred wrappers, public local-file download, server monitor, network monitor, tenant read and metadata add/edit/delete smoke coverage, sys module/menu/button/field write compatibility, mobile module/menu/button write compatibility, and BIZ dictionary maintenance writes through `/dev/dict/add|edit|delete` | Real cloud uploads, config cache mutation, FRM/system dictionary writes, real email/SMS provider sends, provider actions, tenant default-data bootstrap, direct project code generation, scheduler execution, generator config add, data-change events, and cache invalidation hooks are intentionally deferred |
| Business read-only APIs | 92% | Yellow | Product, supplier, warehouse, inventory, delivery, purchase, settlement, payment, expenditure, collection, debit, file relation, history Excel, team project, team-project task user, team-project comment/reply, return order, sale project including cost details, sale-project follow-up, sale-project field change log, sale-project product item relation list, sale-project invoice item page, sale-project draft detail, customer, customer follow-up, sale-project invoicing, invoice, reissue, project-rate page/list/detail, sale-project product info, biz-datareport sale-project summaries/unpaid/details/settlement/sale-profit/summary-statistics reads, leave-application reads, annual-leave balance page/detail, settlement-account-payment reads, payroll reads, workflow process/task reads | Remaining detail consumers |
| Business write APIs | 63% | Red | Customer head reassignment, sale-project invoicing add/edit/delete/complete, sale-project direct invoice add/edit/delete with reverse correction, sale-project direct reissue-order add/edit/delete, sale-project product item/relation mark edits, sale-project product-item standalone add/edit/delete for `FOLLOW` projects, normal sale-project add/edit copied-form product-list mutation, product base add/edit/delete with kit relations, product status/reconciliation, warehouse base add/edit/delete, supplier base add/edit/delete, settlement-account add/edit/status plus quick payment/income, quick expense, transfer add, and ReturnAndRefund correction, payment-record direct add/delete plus payer-time/account corrections, expenditure-record direct add/delete plus payer-time/category/account corrections, collection-receipt direct CRUD, mark-success, and batch expenditure, debit-note direct CRUD, mark-success, batch repayment, and history add, purchase-order direct add/delete, purchase-order cancel status marker, purchase-order edit, purchase-order audit edit, purchase-order warehouse one add, purchase-order warehouse batch add, inventory add registration, inventory zero-count delete, delivery system stocktake add, workflow project-delivery invoice/stock side effects, workflow project-reissue order/product-item side effects, workflow/direct project-return return-order/IN-delivery/inventory side effects plus account-backed workflow return auto-refund, direct return-order edit/delete reverse stock/finance correction, payroll edit/batch-edit/delete, leave-application edit/delete, vacation manual add/edit/logical delete, CC-record current-user add/edit/logical delete, sale-project draft save, sale-project cancel rollback, sale-project repeal discard, sale-project delete logical delete, customer base add/edit/delete, customer follow-up, sale-project follow-up, sale-project product-info, sale-project field-change-log, history-Excel, project-rate, business file-relation binding/delete, team-project base add/edit/delete, team-project member add/manage/edit/delete, team-project comment/reply base writes, team-project task base/category maintenance, team-project task assignee sync, and team-project task comment add/edit/delete are now covered; most side-effect-heavy writes remain deferred by design | Remaining side-effect-heavy routes need Java-public route evidence or explicit product approval before implementation; non-`FOLLOW` product-item mutation, task SSE, and gen-config add remain product-decision territory |
| Frontend adaptation | 98% | Yellow | Original Vue project copied into target repo; request prefix, Bearer token, upload/SSE token headers, local SM2 fallback, double-prefix fix, menu leaf handling adapted, API gap map generated, frontend API method smoke added, org/user display aliases added, paged selector payloads for copied `XnPageSelect`, `XnUserSelector`, and `roleSelectorPlus`, sys/biz organization add/edit/delete actions, sys/biz position add/edit/delete actions, sys/biz user add/edit/delete/grant/status/reset-password actions, sys role add/edit/delete/grant actions, user-center self-service writes, message detail mark-read, homepage all-message mark-read, homepage schedule add/delete, auth monitor session/token exit compatibility, and dev-message send/delete added, sys module/menu/button/field write compatibility added, system menu row field drawer exposed, mobile module/menu/button write compatibility added, gen basic metadata reads/writes, gen basic preview, and gen config edit/delete/editBatch added, auth third user page read added, copy-task CC record reads/delete added, sale-project draft detail read added, annual-leave balance page/detail read and vacation manual maintenance added, sale-project invoice item page read added, sale-project product mark edits added, sale-project field change log reads/writes added, history-Excel reads/writes added, team-project base add/edit/delete added, team-project member add/manage/edit/delete added, team-project task base/category maintenance added, team-project task user reads plus task assignee sync added, team-project task comment add/edit/delete added, team-project comment/reply reads/add/edit/delete added, dev monitor network read added, dev-file LOCAL/dynamic upload plus biz file-relation binding added, sale-project rating detail/add/delete added, product base add/edit/delete and kit relations added, product status/reconciliation added, warehouse base add/edit/delete added, supplier base add/edit/delete added, customer base add/edit/delete and head reassignment added, customer follow-up writes added, sale-project follow-up writes added, sale-project product-info writes added, minimal SSE route added, short-lived SSE client fallback added; browser smoke reaches `/sys/org`, `/sys/user`, `/sys/menu` field drawer, `/mobile/module`, `/mobile/menu` button drawer, `/biz/bizdatareport/summaryStatistics`, `/biz/saleproject` with visible pagination, sale-project detail read tabs, cost tab zero-revenue display, `/dev/file/index` upload, `/biz/bizproduct` XnUpload image upload, `/biz/saleproject/dealProjectList` project attachment upload plus relation binding, `/dev/email/index` TinyMCE rich-text image upload, and temporary `/exm/editor` old `components/Editor` fallback image upload | Remaining side-effect-heavy business routes |
| Testing / QA | 74% | Yellow | Composer, `php think`, route list, PHP lint, smoke tests per slice, frontend API method smoke, user display HTTP smoke, business read HTTP smoke, inventory/delivery read HTTP smoke, inventory add HTTP smoke, delivery add HTTP smoke added pending DB availability, finance read HTTP smoke, purchase-order read HTTP smoke, purchase-order cancel HTTP smoke, purchase-order edit HTTP smoke, purchase-order audit edit HTTP smoke, purchase-order warehouse one add HTTP smoke added pending DB availability, purchase-order warehouse add HTTP smoke added pending DB availability, collection-receipt batch-expenditure HTTP smoke, debit-note batch-repayment HTTP smoke, debit-note history-add HTTP smoke, settlement-account payment/expenses/transfer add HTTP smokes, settlement-account-payment read HTTP smoke, settlement-account read HTTP smoke, supplier/warehouse read HTTP smoke, product read HTTP smoke, HR read HTTP smoke, team-project read HTTP smoke, datareport read HTTP smoke, resource read HTTP smoke, dev read HTTP smoke, dev-config editBatch HTTP smoke, gen read HTTP smoke, gen-basic write HTTP smoke, gen-config write HTTP smoke, tenant write HTTP smoke, CC-record write HTTP smoke, auth/index read HTTP smoke, directory alias HTTP smoke, role selector HTTP smoke, tenant read HTTP smoke, message SSE HTTP smoke, workflow read HTTP smoke, workflow leave start HTTP smoke including generated copy-user CC records, workflow general start HTTP smoke for five non-project first-step starts, workflow payment approve HTTP smoke, workflow payment-out approve HTTP smoke, workflow procure approve HTTP smoke, workflow procure warehouse approve HTTP smoke, workflow project init approve HTTP smoke, workflow project delivery approve HTTP smoke, workflow project play approve HTTP smoke, workflow task transition HTTP smoke including workflow-approved leave payroll generation coverage, workflow process cancel/edit HTTP smoke, backend/frontend browser smoke for summary-statistics, sale-project detail tab service smoke, recoverable message read-state service smokes, sys process-config DB smoke including non-admin rejection, settlement-account base DB smoke, collection-receipt mark-success DB smoke, debit-note mark-success DB smoke, payroll edit/batch-edit/delete DB smoke, payroll import-template service/HTTP smoke, payroll import HTTP smoke added pending DB availability, leave-application edit/delete DB smoke, biz-user-vacation write HTTP smoke, dev-job write HTTP smoke, sale-project draft save DB smoke, gen basic preview service smoke, gen basic ZIP service/HTTP smoke, message SSE process-notice service smoke, dev config `BIZ_DEFINE` DB/HTTP/browser smokes, dev log DB/HTTP smokes, dev job DB/HTTP/browser smokes, gen config editBatch DB/HTTP smokes, sale-project invoicing complete DB/HTTP smokes, sale-project invoicing write HTTP smoke, sale-project invoice add HTTP smoke, sale-project reissue-order add/edit/delete HTTP smoke, dev-file upload/download/delete service and HTTP smokes, dev email/SMS delete DB/HTTP/browser smokes, biz file-relation service and authenticated HTTP smokes, sys module/menu/button/field DB/HTTP smokes, sys role CRUD DB smoke, sys menu field-drawer browser smoke, mobile module/menu/button DB/HTTP smokes, mobile resource browser smoke, team-project base maintenance DB/HTTP smokes, browser upload smokes, rich-text TinyMCE image upload browser smoke, old Editor fallback browser smoke, and BIZ dictionary write smoke | Automated route/API test suite, regression matrix, broader frontend smoke, negative tests |
| Deployment | 22% | Red | Local MySQL/Redis startup method and user-designated runtime target confirmed; env is local and ignored by Git; `scripts/deployment-readiness.ps1` checks PHP/Composer, ThinkPHP files, hidden env-key presence, runtime/storage writable paths, and route boot; `docs/tasks/deployment-server-checklist.md` records the Nginx/PHP-FPM rehearsal checks | Production config values, actual queue/scheduler policy, confirmed server vhost path, backup plan |
| Final online data sync | 0% | Red | Requirement recorded as final-stage reminder | Must design and confirm after project completion; do not start early |

## Completed Compatibility Slices

| Slice | Current Capability | Write Behavior |
| --- | --- | --- |
| Auth | Login, logout, current user, token, RBAC/menu reads, role add/edit/delete, role grant saves, session/token monitor reads and exit compatibility | Partial; password-safe endpoint only, C-side auth and OAuth deferred |
| User directory | Org/user/position selectors, read-only directory, system/business user add/edit/delete, system user import, role/resource/permission grant saves, user enable/disable, admin reset password, user export/download blobs, current-user self-service profile/password/workbench/process-config writes, message detail mark-read, homepage all-message mark-read, and homepage schedule add/delete | Business user import, real Office rendering, and encrypted profile-field migration deferred |
| Workflow reads | Task count/list/page/history, process page/detail/variable reads | First-step runtime writes are tracked in the dedicated workflow runtime row |
| System/dev/mobile/gen reads | Process-config detail/edit, `BIZ_DEFINE` add/edit/delete, dict, file, email/sms records, email/SMS metadata logical delete, job metadata add/edit/delete, logs plus category delete, messages, monitor, resource/menu/mobile resource, gen metadata reads, gen basic add/edit/delete, gen config edit/delete/editBatch, gen basic database metadata, gen basic preview and ZIP download, tenant reads and metadata add/edit/delete | `SYS_BASE` config batch writes/cache mutation, provider send actions, cloud storage, generator config add, direct project code generation, tenant default-data bootstrap, and scheduler execution deferred |
| Business reads/writes | Product, supplier, settlement account, payment record, expenditure record, collection receipt, debit note, payroll, leave application, file relation, team project, task/comments, warehouses, inventory, delivery, purchase order, return order, sale project reads; product status/reconciliation, warehouse base add/edit/delete, supplier base add/edit/delete, settlement-account add/edit/status/payment-add/expenses-add/transfer-add, payment-record direct add/delete and payer-time/account corrections, expenditure-record direct add/delete and payer-time/category/account corrections, collection-receipt direct CRUD/mark-success/batch expenditure, debit-note direct CRUD/mark-success/batch repayment/history add, purchase-order direct add/delete/cancel status marker/edit/audit-edit/warehouse-one-add/warehouse-add, inventory add registration/zero-count delete, delivery system stocktake add, payroll edit/batch-edit/delete, leave-application edit/delete, customer base add/edit/delete, customer follow-up, sale-project follow-up, sale-project add/edit copied-form product-list mutation, sale-project deal edit delivery/freight maintenance, sale-project cancel rollback, sale-project repeal discard, sale-project delete logical delete, product-info, field-change-log, history-Excel, project-rate, business file-relation add/edit/delete/project-case delete, CC-record current-user add/edit/delete, team-project base add/edit/delete, team-project member add/manage/edit/delete, and team-project comment/reply base writes | Most side-effect-heavy business writes deferred |
| SSE compatibility | `/dev/message/createSseConnect` Java/frontend behavior mapped, minimal protected ThinkPHP route added, frontend short-lived reconnect fallback added, and the compatibility stream now emits initial `FlushMessageNotice` plus `FlushProcessNotice` events | Full realtime push, Redis pub/sub fanout, and workflow-triggered push deferred |
| Sys field resource maintenance | `/sys/field/page`, `/tree`, `/detail`, `/MenuTreeSelector`, `/add`, `/edit`, and `/delete` routed for copied system field resource drawers, reading and writing `sys_resource.CATEGORY = FIELD`; writes require a menu parent, reject duplicate sibling field codes, logically delete active rows, tolerate mixed missing ids, and remove direct role-resource relation rows targeting deleted fields | Java data-change events, cache invalidation hooks, and any field permission model beyond direct target relations deferred |
| Gen basic metadata maintenance | `/gen/basic/tables`, `/tableColumns`, `/add`, `/edit`, and `/delete` routed for copied generator basic form metadata, reading MySQL `information_schema`, excluding `ACT_` workflow engine tables, creating default `gen_config` rows from selected table columns, preserving config edits unless the target table changes, refreshing primary-key flags, and logically deleting basic/config rows after full-batch validation | `execGenPro`, templates, generated code output, menu/resource generation, and schema writes deferred |
| Gen basic preview and ZIP download | `/gen/basic/previewGen` routed for copied generator preview modal, returning Java-compatible SQL/frontend/backend/mobile buckets with `codeFileName`, `codeFileWithPathName`, and `codeFileContent`; `/gen/basic/execGenZip` reuses those buckets for an authenticated temporary ZIP blob without writing project files; service and HTTP smoke verified valid ZIP output, missing-id 404, no-token `code=401`, and stable `gen_basic`/`gen_config` row counts | `execGenPro`, direct project writes, and full Java Beetl template parity deferred |
| Gen config metadata saves | `/gen/config/list`, `/detail`, `/edit`, `/delete`, and `/editBatch` routed for copied generator field-config component; edit and editBatch accept Java-style metadata fields, validate target rows before writing, update only Java edit-parameter columns, ignore client audit/delete fields, reject deleted rows, return `data = null`, and delete logically marks selected config rows after full-batch validation; editBatch was DB/HTTP-smoked on 2026-06-08 and edit/delete were HTTP-smoked on 2026-06-16 | Direct `/gen/config/add`, execution, templates, and generated code output deferred |
| Auth third user page read | `/auth/third/page` routed for copied third-party user binding pagination, reading `auth_third_user` with category/search filters | OAuth render/callback, third-party binding writes, user creation, and token issuing deferred |
| Sale project reads and narrow writes | `/biz/saleproject/page`, `/case/page`, `/operation/page`, `/public/page`, `/list/detail`, `/detail`, `/product`, `/cost`, `/cost/details`, `/add`, `/edit`, `/history/add`, `/special/add`, `/visibility/edit`, `/amount/edit`, `/deal/edit`, `/cancel`, `/repeal`, and `/delete` routed with aggregate read data, weighted-average purchase cost details, route precedence fixed for completed-project cost tabs, admin-compatible list data scope for the copied sale-project page, base/history/reimbursement creation, copied-form product-list mutation for normal add/edit, visibility/specimen maintenance, amount/status/totals correction, delivery/freight field maintenance, WAIT_DELIVER-to-FOLLOW rollback with invoicing logical delete, FOLLOW-to-DISCARD discard maintenance, FOLLOW-only logical delete maintenance, workflow project-delivery inventory side effects, workflow project-reissue order/product-item side effects, workflow project-return return-order/IN-delivery side effects, standalone product-item add/edit/delete for `FOLLOW` projects, and direct reissue-order add/edit/delete for active non-`FOLLOW` projects | Finance side effects beyond amount/return correction and project collection, notifications, non-`FOLLOW` direct product-item mutation, and Java data-change events deferred |
| Sale project follow-up reads and writes | `/biz/saleprojectfollowup/page`, `/detail`, `/add`, `/edit`, and `/delete` routed with project name, creator display fields, data-scope guarding, `fileList` to `extJson` preservation, and logical delete | File upload/storage cleanup, notifications, sale-project state, workflow, finance, and inventory side effects deferred |
| Sale project product item relation reads and mark/standalone edits | `/biz/saleprojectproductitemrelation/list`, `/biz/saleprojectproductitemrelation/mark/edit`, `/biz/saleprojectproductitem/mark/edit`, `/biz/saleprojectproductitem/add`, `/edit`, and `/delete` routed with smoke-covered combo-product child relation reads, product aliases, product display fields, data-scope guarding through the owning sale project, single-field `MARK` writes, and `FOLLOW`-only standalone product-item maintenance. Normal sale-project add/edit product-list mutation is covered through `/biz/saleproject/add|edit` | Non-`FOLLOW` product-item mutation, delivery, invoice, inventory, workflow, finance, and sale-project state side effects deferred |
| Sale project detail browser smoke | `/biz/saleproject` detail modal read tabs browser-smoked for project information, follow-up records, case empty state, and pending-process empty state; follow-up add backend write is now available | Case upload/add, workflow actions, and sale-project writes deferred |
| Sale project cost frontend display | Completed-project cost tab guards zero-revenue gross profit rate and displays numeric zero instead of `NaN%` | Backend cost payloads and sale-project/finance writes unchanged |
| Sale project remaining tab smoke | Payment, return-order, invoice, and file-relation tab read services verified against one imported project | Browser visual confirmation and all write controls deferred |
| Product reads and base writes | `/biz/bizproduct/page`, `/list`, `/detail`, `/children`, `/add`, `/edit`, `/delete`, `/edit/status`, and `/reconciliation/edit` routed with product/org display aliases, base product maintenance, kit `KIT_PRODUCT_DATA` relation maintenance, status toggling, selected-product reconciliation fields, write-scope guarding, audit fields, and logical delete | Inventory, purchase, sale-project, finance transaction, workflow, file upload/storage implementation, and data-change/cache events deferred |
| Warehouse reads and base writes | `/biz/warehouses/page`, `/list`, `/detail`, `/add`, `/edit`, and `/delete` routed with owner/org display aliases, token owner/org defaults, write-scope guarding, and logical delete | Inventory stock updates, delivery records, purchase-order writes, sale-project invoice writes, and workflow side effects deferred |
| Supplier reads and base writes | `/biz/supplier/page`, `/list`, `/list/query/name`, `/detail`, `/add`, `/edit`, and `/delete` routed with supplier display aliases, lower-case physical `org` preservation, write-scope guarding, and logical delete | Supplier import/export and purchase/payment/procurement/inventory/workflow side effects deferred |
| Customer reads and base/follow-up/head writes | `/biz/customer/page`, `/add`, `/edit`, `/delete`, `/head/edit`, `/detail`, `/detail/list`, `/biz/customerfollowup/page`, `/detail`, `/add`, `/edit`, and `/delete` routed with customer owner/org/file, customer base write guarding, head reassignment, follow-up creator display fields, data-scope write guarding, and logical delete | Attachment upload/storage cleanup, notifications, data-change events, sale-project/customer side effects, and SM4 plaintext search deferred |
| Sale project billing reads/writes | `/biz/saleprojectinvoicing/page`, `/customer`, `/detail`, `/add`, `/edit`, `/delete`, `/complete`, `/biz/saleprojectinvoice/page`, `/list`, `/add`, `/edit`, `/delete`, `/biz/saleprojectinvoiceItem/page`, `/biz/saleprojectreissueorder/list/query`, `/biz/saleprojectreissueorder/add`, `/edit`, `/delete`, `/biz/projectrate/page`, `/list`, `/detail`, `/add`, `/edit`, and `/delete` routed with nested invoice/reissue structures, smoke-covered read contracts, invoicing row maintenance and complete marker, direct invoice add/edit/delete with product-item delivery/project-state correction and delete-time reverse correction, direct reissue-order add/edit/delete with project totals/status correction and workflow/delivered/reference guards, rating detail reads, base rating logical delete, workflow-owned delivery invoice writes through `Process_sale_project_delivery` approval, workflow-owned reissue-order/product-item writes through `Process_project_reissue_product` approval, workflow/direct return-order IN delivery/inventory writes, direct return-order edit/delete reverse stock/finance correction, account-backed workflow return auto-refund, and ReturnAndRefund expense correction | Image storage and broader finance writes deferred. No-account return auto-refund is not a Java-parity target unless requested as new product behavior |
| Biz directory aliases | `/biz/org`, `/biz/user`, `/biz/position`, and `/biz/dict` read paths routed to existing system/dev read services; `/biz/org/add`, `/biz/org/edit`, `/biz/org/delete`, `/biz/position/add`, `/biz/position/edit`, `/biz/position/delete`, `/biz/user/add`, `/biz/user/edit`, `/biz/user/center/edit`, `/biz/user/delete`, `/biz/user/grantRole`, `/biz/user/disableUser`, `/biz/user/enableUser`, `/biz/user/resetPassword`, `/biz/user/export`, `/biz/user/exportUserInfo`, and `/biz/dict/edit` routed for copied personnel and business-dictionary page actions | Dict add/delete, business user import, and real Office rendering deferred |
| Organization maintenance writes | `/sys/org/add`, `/sys/org/edit`, `/sys/org/delete`, `/biz/org/add`, `/biz/org/edit`, and `/biz/org/delete` routed with category validation, duplicate-name checks, parent cycle prevention, business data-scope guards, dependency-protected logical delete, and copied frontend payload compatibility | Java data-change events, physical delete behavior, and unrelated user side effects deferred |
| Position maintenance writes | `/sys/position/add`, `/sys/position/edit`, `/sys/position/delete`, `/biz/position/add`, `/biz/position/edit`, and `/biz/position/delete` routed with category validation, duplicate-name checks, business data-scope guards, user-reference-protected logical delete, and copied frontend payload compatibility | Java data-change events and physical delete behavior deferred |
| Workflow read aliases and first runtime writes | `/biz/process/all/page`, `/query`, guarded `/query/list`, `/variable`, `/project/runtime/query/list`, `/fileList`, and `/biz/task/runtime/activity/detail` routed through Camunda-table read services; `POST /biz/process/leave/start` creates a minimal `Process_ask_leave` runtime/history row set plus generated copy-user CC rows and workflow file relations; `POST /biz/process/payment/start`, `/reimbursement/start`, `/makePayment/start`, `/procure/start`, and `/procure/warehouse/start` create minimal first-step runtime/history rows plus generated copy-user CC rows and workflow file relations; `POST /biz/process/project/init/start` creates `Process_sale_project_init` first-step rows and moves the sale project to `PENDING_APPROVAL`; `POST /biz/process/project/play/start` creates `Process_sale_project_play` first-step rows and its approval path advances into `Activity_payment_approval` before finance approval applies project collection side effects; `POST /biz/process/cancel|leave/edit` covers active leave cancellation/editing before approval, and cancel also covers active unapproved non-project first-step, project-init, and project-play workflows; `POST /biz/task/approve|reject` closes minimal `Process_ask_leave` `Activity_approval` tasks into history; `Process_payment` approve closes workflow history and performs payment-in settlement side effects; `Process_reimbursement`/`Process_make_payment` first approval creates `Activity_pay_approval`, and finance approval performs payment-out settlement side effects; `Process_procure` approval advances through procurement confirmation and optional general-office approval before creating purchase-order rows; `Process_procure_in_warehouse` approve closes workflow history and performs purchase-order warehouse-in side effects; `Process_sale_project_init` approve writes bounded sale-project init side effects while reject/cancel roll back to `FOLLOW`; `Process_sale_project_play` finance approval writes project payment rows and recalculates project payment status while reject/cancel close without finance side effects; approved leave transitions create/read back one `biz_leave_application` row; approved `annualLeave` transitions deduct current-year vacation balances; approved `leaveOfAbsence` rows are consumed by explicit payroll generation; direct leave edit/delete adjusts current-year annual-leave balances; workflow list/detail/start/transition/cancel/edit smokes pass | Standalone `/biz/task/sse/stream`, remaining project workflow starts/approvals for delivery, reissue, and return, non-leave process edit, automatic existing-payroll row updates, and SSE push deferred |
| Sale project product info reads and writes | `/biz/saleprojectproductinfo/page`, `/list`, `/detail`, `/add`, `/edit`, and `/delete` routed with smoke-covered software package/version reads, creator display names, audit writes, and logical delete | Product master-data, product-item, import/export, report, workflow, finance, inventory, and delivery side effects deferred |
| Biz datareport sale-project details | `/biz/bizdatareport/saleProjectList/details` routed with sale project rows, nested product items, package children, and return orders | Other report, profit, unpaid-payment, and summary-statistics endpoints deferred |
| Biz datareport sale-project summaries | `/biz/bizdatareport/saleproject`, `/saleproject/list`, `/saleproject/report`, and `/saleproject/UnpaidPayment` routed with amount totals, project amount rows, status/time rows, and unpaid amount | Summary-statistics endpoint deferred |
| Biz datareport settlement reads | `/biz/bizdatareport/settlement/income` and `/settlement/expenses` routed with payment/expenditure record rows, settlement categories, payer time, account, org, and amount fields | Summary-statistics, settlement mutations, and account balance updates deferred |
| Biz datareport sale-profit read | `/biz/bizdatareport/saleProfit` routed with raw `projectlist`, `orderList`, and `bizProducts` collections for frontend WebWorker calculation | Purchase/sale/return/inventory mutations and workflow side effects deferred |
| Biz datareport summary-statistics read | `/biz/bizdatareport/summary/statistics` routed with company-scoped `org`, `settlementAccounts`, `paymentRecords`, `bizExpenditureRecords`, `bizSaleProjects`, and `bizDebitNotes` collections for frontend WebWorker calculation; copied Vue page browser-smoked at `/biz/bizdatareport/summaryStatistics` | Settlement/account-balance mutations and workflow side effects deferred |
| Leave application reads and base writes | `/biz/bizleaveapplication/page`, `/my/page`, `/detail`, `/add`, `/edit`, and `/delete` routed with applicant, organization, workflow process id, date, amount, and object id fields; direct add writes one guarded manual leave row, rejects overlapping active ranges, and deducts current-year `annualLeave` balance; edit writes only Java edit-param fields, delete validates all ids before soft delete, approved leave workflow transitions now generate the leave row, editable workflow variables feed the final row, approved `annualLeave` transitions deduct vacation balance, approved `leaveOfAbsence` rows feed explicit payroll generation, and direct edit/delete adjusts current-year annual-leave balances | Automatic existing-payroll row recalculation and broader workflow side effects deferred |
| Settlement account payment reads | `/biz/settlementaccountpayment/page` and `/list` routed with account statement amount, settlement type/category, process id, account, and timestamp fields | Statement reads are covered; quick income, quick expense, and transfer creation are covered by settlement-account add slices |
| Settlement account quick payment add | `/biz/settlementaccount/payment/add` routed with Java-compatible quick-income creation: statement row, payment record, and account balance increment in one transaction | Java data-change events, workflow hooks, and delete remain deferred |
| Settlement account quick expenses add | `/biz/settlementaccount/expenses/add` routed with Java-compatible quick-expense creation: statement row, expenditure record, and account balance decrement in one transaction | Java data-change events, workflow hooks, collection-receipt settlement propagation, and delete remain deferred |
| Settlement account transfer add | `/biz/settlementaccount/transfer/add` routed with Java-compatible account transfer creation: two statement rows, one expenditure record, one payment record, fixed `dealings` category, and two-account balance movement in one transaction | Java data-change events, workflow hooks, collection-receipt/debit-note settlement propagation, and delete remain deferred |
| Collection receipt direct CRUD | `/biz/bizcollectionreceipt/add`, `/edit`, and `/delete` routed for bounded product CRUD: validates active payment-record binding, amount and settlement bounds, one active receipt per payment record, settled-row remark-only edits, and logical delete only for rows without settlement/settled status | Settlement-account balance changes and repayment expenditure/statement creation outside the batch-expenditure route deferred |
| Collection receipt mark-success | `/biz/bizcollectionreceipt/mark/success/edit` routed for copied collection-receipt page actions, updating only `PLAY_STATUS`, audit fields, and `VERSION`; DB smoke verified missing-id rejection, non-admin rejection, version increment, unchanged amount/payment fields, and no settlement account, statement, payment, or expenditure side effects | Settlement-account balance changes and expenditure-record creation outside the batch-expenditure route deferred |
| Collection receipt batch expenditure | `/biz/bizcollectionreceipt/batchExpenditure/edit` routed with Java-compatible repayment quick-settlement creation: validates selected receipts/items, creates repayment expenditure/statement rows through the settlement-account quick-expense path, updates receipt settlement amount/status/version, and decrements the chosen settlement account | Java event bus, workflow hooks, and broader finance delete behavior remain deferred |
| Debit note direct CRUD | `/biz/bizdebitnote/add`, `/edit`, and `/delete` routed for bounded product CRUD: validates active expenditure-record binding, amount and settlement bounds, one active debit note per expenditure record, account-derived org on add, settled/history remark-only edits, and logical delete only with zero settlement/history amounts plus non-settled status | Payment/statement creation outside the batch-repayment route, Java event bus, workflow hooks, and broader finance delete behavior deferred |
| Debit note mark-success | `/biz/bizdebitnote/mark/success/edit` routed for copied debit-note page actions, updating only `PLAY_STATUS`, audit fields, and `VERSION`; DB smoke verified missing-id rejection, non-admin rejection, version increment, unchanged amount/history/expenditure/org fields, and no settlement account, statement, payment, or expenditure side effects | Settlement-account balance changes and payment-record creation outside the batch-repayment route deferred |
| Debit note batch repayment | `/biz/bizdebitnote/batchRepayment/edit` routed with Java-compatible loan-repayment quick-settlement creation: validates selected notes/items, creates `LoanRepayment` payment/statement rows through the settlement-account quick-income path, updates debit-note settlement amount/status/version, and increments the chosen settlement account | Java event bus, workflow hooks, broader finance delete behavior, and collection-receipt settlement remain deferred |
| Debit note history add | `/biz/bizdebitnote/history/add` routed with Java-compatible historical debit-note creation: validates account/amount/history/create-time input, derives org/tenant from the selected settlement account, inserts one debit-note row with no expenditure link, and preserves account balances plus payment/expenditure/statement rows | Java event bus, workflow hooks, broader finance delete behavior, and collection-receipt settlement remain deferred |
| Payroll reads, template/export download, generation, import, and base writes | `/biz/bizpayroll/page`, `/mypage`, `/detail`, `/add`, `/downloadImportTemplate`, `/export`, `/generate/add`, `/import`, `/edit`, `/bath/edit`, and `/delete` routed with salary fields, employee display name, organization display name, salary month filters, data-scope guards, direct manual payroll-row add for active user/org pairs, original Java import-template bytes, CSV export blob, Java-compatible salary generation formulas, workflow-approved leave-of-absence consumption, focused Java-template payroll import, Java edit-field-only updates, full-batch validation before batch writes, and logical delete; DB/HTTP smoke verified preserved non-edit fields, missing-id rollback, non-admin rejection, deleted detail hiding, stable payroll row count, template SHA `4A98E66E74E8D310D6226A5F6DD60602652FC25FD6D0FB272281BBF19CD861B8`, export CSV marker/header, no-token export rejection, and no related-table export side effects; payroll direct add/generate/import DB smokes are covered by focused scripts | EasyExcel-style xlsx export rendering, automatic existing-payroll row updates on workflow approval, Java data-change events, and duplicate-month prevention deferred |
| Sys user add/edit/delete/import/grants/status/reset/export and user-center/index writes | `/sys/user/add`, `/sys/user/edit`, `/biz/user/add`, `/biz/user/edit`, `/sys/user/list/detail`, `/ownRole`, `/ownResource`, `/ownPermission`, `/sys/user/delete`, `/biz/user/delete`, `/sys/user/import`, `/sys/user/grantRole`, `/biz/user/grantRole`, `/sys/user/grantResource`, `/sys/user/grantPermission`, `/sys/user/disableUser`, `/sys/user/enableUser`, `/biz/user/disableUser`, `/biz/user/enableUser`, `/sys/user/resetPassword`, `/biz/user/resetPassword`, `/sys/user/downloadImportUserTemplate`, `/sys/user/export`, `/sys/user/exportUserInfo`, `/biz/user/export`, and `/biz/user/exportUserInfo` routed for copied user add/edit/delete/import/grant/status/reset/download dialogs; `/sys/userCenter/updatePassword`, `/updateAvatar`, `/updateSignature`, `/updateUserInfo`, `/updateUserWorkbench`, `/process/config/edit`, detail-time message mark-read, `/sys/index/message/allMessageMarkRead`, `/sys/index/schedule/add`, and `/sys/index/schedule/deleteSchedule` routed for current-user self-service flows | Business user import and real Office rendering deferred |
| System role maintenance | `/sys/role/add`, `/edit`, and `/delete` routed for copied role management consumers, writing `sys_role` rows with Java-compatible `GLOBAL`/`ORG` categories, generating server-side random role codes, rejecting duplicate same-org names, rejecting missing `orgId` for organization roles, protecting `superAdmin`/`tenantAdmin` from edit/delete, soft-deleting roles, and clearing user/resource/mobile-menu/permission relations | Data-change events, cache invalidation hooks, and route-permission middleware deferred |
| System process-config maintenance | `/sys/sysConfig/detail` and `/edit` routed for copied process-configuration page, normalizing known process keys and saving Java-compatible `CONFIG_JSON` into the current tenant `sys_config` row with admin-compatible write guard | `generateConfig`, cache invalidation hooks, route-permission middleware, and optimistic-lock behavior deferred |
| Dev message management | `/dev/message/page`, `/detail`, `/send`, `/createSseConnect`, and `/delete` routed for copied station-message management page; detail marks the current receiver relation as read, send creates message and receiver relations, delete removes receiver relations and selected message rows with conservative owner/admin guard, and short SSE connect emits message/process refresh notices | Full SSE/WebPush realtime push, Redis fanout, and attachment/storage cleanup deferred |
| Dev file local upload/download/delete and relation binding | `/dev/file/uploadDynamicReturnId`, `/uploadDynamicReturnUrl`, `/uploadDynamicReturnFile`, `/uploadLocalReturnId`, `/uploadLocalReturnUrl`, `/uploadLocalReturnFile`, `/dev/file/delete`, `/dev/file/download`, `/biz/bizfilerelation/add`, `/edit`, `/delete`, and `/projectCase/del` routed for copied frontend upload controls, metadata logical delete, local file download, and business attachment binding; LOCAL uploads write `runtime/upload/dev_file/defaultBucketName/yyyy/M/d`, insert `dev_file`, return Java-compatible id/url/file data, delete sets `dev_file.DELETE_FLAG = DELETED` without physical cleanup, relation add copies `dev_file.NAME`, and cloud upload routes return `501` unsupported instead of fake success; browser-smoked `/dev/file/index`, product `XnUpload`, sale-project attachment binding on 2026-06-08, email TinyMCE rich-text image upload on 2026-06-11, and old `components/Editor` fallback upload through temporary `/exm/editor` on 2026-06-12 | Real cloud engine uploads/downloads, thumbnail generation, and optional physical file cleanup remain deferred |
| Dev email/SMS record delete | `/dev/email/page`, `/detail`, `/delete`, `/dev/sms/page`, `/detail`, `/sendAliyun`, `/sendTencent`, `/sendXiaonuo`, and `/delete` routed for copied email/SMS pages; delete accepts Java-style `[{id}]` payloads, rejects malformed mixed payloads, stays tenant-scoped, sets `DELETE_FLAG = DELETED`, and returns `data = null`; SMS send wrappers return controlled deferred responses; DB, HTTP, and browser-smoked for record pages/deletes on 2026-06-08 | Email provider send routes, real SMS provider sends, provider credentials, rate limits, notification side effects, and external delivery calls remain deferred |
| Dev config safe maintenance | `/dev/config/page`, `/list`, `/detail`, `/add`, `/edit`, `/editBatch`, and `/delete` routed for copied configuration pages; add/edit/delete only allow `BIZ_DEFINE`, reject duplicate keys and `SYS_BASE` single-row writes, preserve existing sensitive values when edit submits `******`, reject malformed mixed delete payloads before any write, set `DELETE_FLAG = DELETED`, and return `data = null`; editBatch updates only existing active `CONFIG_VALUE` rows in a validated transaction and preserves masked sensitive values; DB, HTTP, and browser-smoked on 2026-06-08 plus editBatch HTTP-smoked on 2026-06-16 | Redis config cache mutation, provider send/test behavior, and unmasking sensitive values remain deferred |
| Dev log category delete | `/dev/log/page`, `/detail`, `/vis/*`, `/op/*`, and `/delete` routed for copied log API wrappers; delete accepts `{ category }`, rejects empty category, physically deletes target-category rows, and applies current-tenant protection when a token tenant id is available; DB and HTTP-smoked on 2026-06-08 | Copied target frontend currently has no `src/views/dev/log` page to browser-smoke; Java global category clear differs from ThinkPHP tenant-protected clear |
| Dev job metadata and status maintenance | `/dev/job/page`, `/list`, `/detail`, `/getActionClass`, `/add`, `/edit`, `/delete`, `/stopJob`, `/runJob`, and `/runJobNow` routed for copied scheduled-job page; add/edit validate Java-style fields, `FRM`/`BIZ` category, cron text shape, action-class allow-list, duplicate active `ACTION_CLASS + CRON_EXPRESSION`, and running-edit guard; add creates `STOPPED` jobs, edit preserves code/status, delete accepts Java-style `[{id}]` with malformed mixed payload protection, and job actions update only `JOB_STATUS` with Java-style already-running/already-stopped guards | Scheduler registration/removal, task class execution, full Java bean validation, and scheduler lifecycle remain deferred |
| Biz CC records current-user maintenance | `/biz/ccrecords/page`, `/detail`, `/add`, `/edit`, and `/delete` routed for copied workflow copy-task consumers, filtered to the current token user, enriched with promoter/user display names, forcing add `USER` to the current user, preserving user/tenant/delete fields on edit, logically deleting by current user only, and reading generated active leave/non-project/project-init/project-delivery/project-play `copyUserIdList` rows | Copy generation for remaining project process keys, notifications, data-change events, and approval actions outside the bounded workflow runtime deferred |
| Biz draft detail read and save | `/biz/bizdraft/detail` and `/biz/bizdraft/saleproject/add` routed for copied sale-project draft reload/save flows, reading by `TARGET_ID`, preserving raw `extJson`, creating `CATEGORY = SALE_PROJECT_INIT`, and overwriting the existing active draft in the current tenant | Sale-project writes, workflow start, and file upload/storage side effects deferred |
| Biz user vacation reads and manual maintenance | `/biz/bizuservacation/page`, `/detail`, `/add`, `/edit`, and `/delete` routed for copied vacation-balance consumers; detail defaults to current user and `annualLeave`, add/edit validate user, tenant, category length, duplicate current-year rows, nonnegative amounts, and `usedAmount <= amount`, delete logically marks rows after full-batch validation, approved `annualLeave` workflow transitions increment `USED_AMOUNT`, editable workflows deduct the edited amount at approval, and direct leave edit/delete adjusts/restores current-year annual leave | Vacation generation, automatic existing-payroll row recalculation, notification, and data-change side effects deferred |
| Biz history Excel reads and writes | `/biz/bizhistoryexcel/page`, `/detail`, `/add`, `/edit`, and `/delete` routed for copied historical spreadsheet data browsing and base row persistence, preserving raw `extJson` and using logical delete | Import/export, spreadsheet parser changes, `biz_history_excel_row` writes, and storage changes deferred |
| Sale project invoice item page read | `/biz/saleprojectinvoiceItem/page` routed for copied sale-project delivery invoice item pagination, with product and warehouse display aliases | Standalone invoice item writes remain deferred; direct invoice add/edit/delete is covered through `/biz/saleprojectinvoice/add|edit|delete`, and workflow-owned project delivery writes are covered through `Process_sale_project_delivery` approval |
| Sale project field change log reads and writes | `/biz/salesprojectfieldchangelog/page`, `/detail`, `/add`, `/edit`, and `/delete` routed for copied sale-project change-log browsing and base log-row maintenance, with project and creator display aliases plus logical delete | Sale-project generated change writes, workflow, finance, and audit side effects deferred |
| Team project base maintenance | `/biz/bizteamproject/add`, `/edit`, and `/delete` routed for copied project card/detail consumers, creating the current user as project owner and `LEADER`, syncing `TEAM_PROJECT_USER_HAS_RESOURCE_PERMISSION`, updating base fields, and logically deleting projects plus active member rows | Member role edit, notification push, data-change events, and Java physical delete behavior deferred |
| Team project member maintenance | `/biz/bizteamprojectuser/add`, `/manage/add`, `/edit`, and `/delete` routed for copied/Java team-project member consumers, requiring imported project resource permissions for add/manage/delete, restoring previously deleted rows, syncing member relation permission JSON for add/manage, refreshing audit fields only on Java-compatible edit, and logically deleting members | Role-changing edit, notification push, and data-change events deferred |
| System button resource maintenance | `/sys/button/add`, `/edit`, and `/delete` routed for copied system resource button consumers, writing `sys_resource` rows with `CATEGORY=BUTTON`, checking duplicate button `CODE`, refreshing audit fields, logically deleting buttons, and removing deleted ids from role-resource `EXT_JSON.buttonInfo` | Data-change events and cache invalidation hooks deferred |
| System menu resource maintenance | `/sys/menu/add`, `/edit`, `/changeModule`, and `/delete` routed for copied system resource menu consumers, writing `sys_resource` rows with `CATEGORY=MENU`, requiring Java menu form fields, normalizing `MENU`/`IFRAME`/`LINK`/`CATALOG` fields, rejecting duplicate sibling titles, validating parent/module consistency, rejecting self/descendant parent edits, changing only root menu trees' `MODULE`, logically deleting menu/button/field trees, deleting role-resource relation rows, and exposing the copied `字段权限` drawer from MENU rows | Data-change events and cache invalidation hooks deferred |
| System module resource maintenance | `/sys/module/add`, `/edit`, and `/delete` routed for copied system resource module consumers, writing `sys_resource` rows with `CATEGORY=MODULE`, checking duplicate module `TITLE`, generating Java-style random `CODE`, rejecting built-in module delete, logically deleting modules and child menu/button/field resources, and clearing role-resource relations | Data-change events and cache invalidation hooks deferred |
| Mobile module resource maintenance | `/mobile/module/add`, `/edit`, and `/delete` routed for copied mobile resource module consumers, writing `mobile_resource` rows with `CATEGORY=MODULE`, checking duplicate mobile module `TITLE`, generating Java-style random `CODE`, logically deleting modules and module-owned menu trees, tolerating mixed missing ids on delete, and deleting role mobile-menu relation rows | Button cleanup on module delete, data-change events, and cache invalidation hooks deferred |
| Mobile menu resource maintenance | `/mobile/menu/add`, `/edit`, `/changeModule`, and `/delete` routed for copied mobile resource menu consumers, writing `mobile_resource` rows with `CATEGORY=MENU`, requiring Java menu form fields, rejecting duplicate sibling titles, validating parent/module consistency, rejecting self/descendant parent edits, changing only root menu trees' `MODULE`, logically deleting menu trees, deleting role mobile-menu relation rows, and preserving mobile button rows | Mobile role grant writes, data-change events, and cache invalidation hooks deferred |
| Mobile button resource maintenance | `/mobile/button/add`, `/edit`, and `/delete` routed for copied mobile resource button consumers, writing `mobile_resource` rows with `CATEGORY=BUTTON`, checking duplicate mobile button `CODE`, inheriting tenant id from the parent mobile menu, logically deleting buttons, tolerating mixed missing ids on delete, and removing deleted ids from role mobile-menu `EXT_JSON.buttonInfo` | Data-change events and cache invalidation hooks deferred |
| Team project task user reads and assignee sync | `/biz/bizteamprojecttaskuser/page` and `/detail` routed for copied team-task member browsing, and `/biz/bizteamprojecttask/user/edit` routed for task detail assignee sync with project-member, imported `addUser`, and task `MANAGE` guards | Standalone task-user add/edit/delete, task status/progress/content writes, notifications, and data-change events deferred |
| Team project task category maintenance | `/biz/bizteamprojecttaskcategory/add`, `/edit`, `/sort/edit`, and `/delete` routed for copied kanban category consumers, requiring project maintainer permission, defaulting new category `SORT_CODE` to `99`, and rejecting non-empty category deletion | Task add/edit/delete, task drag/move, notifications, and data-change events deferred |
| Team project task base maintenance | `/biz/bizteamprojecttask/add`, `/edit`, and `/delete` routed for copied kanban task consumers, creating current-user `MANAGE` task membership, validating selected task users as project members, validating task status, and logically deleting tasks plus active task-user rows | Generated task logs, notifications, data-change events, workflow actions, and full drag ordering deferred |
| Team project task comment maintenance | `/biz/bizteamprojecttaskcomment/add`, `/edit`, and `/delete` routed for copied task comment consumers, deriving the team project from the task, preserving `EXT_JSON.file` attachment metadata, and protecting generated `LOG` rows as read-only | Notifications, data-change events, and generated task-log maintenance deferred |
| Team project task complete write smoke | `scripts/team-project-task-write-http-smoke.ps1` now verifies temporary project/member setup, category/task/task-user/task-comment writes, validation guards, logical-delete hiding, final DB state, and cleanup through HTTP | Notification push, realtime updates, generated task logs, workflow actions, data-change events, and full drag ordering still require separate designs |
| Team project comment/reply reads and writes | `/biz/bizteamprojectcomment/detail`, `/add`, `/delete`, `/biz/bizteamprojectcommentreply/page`, `/detail`, `/add`, `/edit`, and `/delete` routed for copied project timeline comment/reply consumers, preserving project-member visibility guard, imported `delComment` permission for maintenance, and storing mentioned users under `EXT_JSON` | Notifications and data-change events deferred |
| Dev monitor network read | `/dev/monitor/networkInfo` routed for copied monitor pages, returning `upLinkRate` and `downLinkRate` with safe zero fallback | Monitor writes, server control, and metric persistence deferred |
| Sale project rating reads and writes | `/biz/projectrate/page`, `/list`, `/detail`, `/add`, `/edit`, and `/delete` routed for copied sale-project rating consumers, returning `projectName`, `customerName`, raw `extJson`, preserving `imgList`, and using logical delete | Image upload/storage, sale-project writes, workflow, finance, and notifications deferred |
| Biz user vacation write smoke | `scripts/biz-user-vacation-write-http-smoke.ps1` verifies no-token rejection, temporary add/detail, duplicate rejection, edit `VERSION` increment, invalid amount rejection, mixed delete rollback guard, logical delete hiding, and unchanged leave/payroll counts; `scripts/workflow-task-transition-http-smoke.ps1` verifies workflow-owned `annualLeave` deduction; `scripts/workflow-process-cancel-edit-http-smoke.ps1` verifies cancel leaves vacation unchanged and editable approval deducts the edited amount; `scripts/biz-leave-application-vacation-adjustment-http-smoke.ps1` verifies direct leave edit/delete annual-leave adjustment | Generation helpers, automatic existing-payroll row recalculation, and broader side effects deferred |
| Test-agent smoke automation | `scripts/test-agent-smoke.ps1` and `docs/tasks/test-agent-smoke-runbook.md` added for Composer autoload, ThinkPHP bootstrap, route coverage, strict PHP lint, frontend API method smoke, whitespace checks, optional no-token backend smoke, optional dev-config HTTP smoke, optional dev-log HTTP smoke, optional dev-job HTTP smoke, optional gen-config HTTP smoke, optional sale-project invoicing HTTP smoke, optional dev-file delete HTTP smoke, optional dev-email/SMS HTTP smoke, optional file-relation HTTP smoke, optional sys-module/sys-menu/sys-button/sys-field/mobile-module/mobile-menu/mobile-button HTTP smoke, optional team-project base HTTP smoke, and DB-backed user export/dev-config/dev-log/dev-job/gen-config/sale-project-invoicing/file upload/file-delete/file-relation/sys-module/sys-menu/sys-button/sys-field/mobile-module/mobile-menu/mobile-button/team-project service smoke against the user-provided local runtime | Browser smoke for copied Vue export/download buttons, dev-log page if restored, generator config nested form save, sale-project invoicing complete button, sys/mobile resource add/edit/delete flows, and team-project visible add/edit/delete flow |

Browser dev-file delete smoke on 2026-06-08 verified `/dev/file/index` upload followed by row delete: upload posted to `/api/dev/file/uploadLocalReturnUrl`, delete posted to `/api/dev/file/delete`, the temporary row reached `DELETE_FLAG = DELETED`, the table refreshed, and temporary DB/disk artifacts were cleaned.

Browser dev-config smoke on 2026-06-08 verified `/dev/config` other-config add/edit/delete: add posted to `/api/dev/config/add`, edit posted to `/api/dev/config/edit`, delete posted to `/api/dev/config/delete`, responses returned `code=200` with `data = null`, the edited database value changed to `value-b`, the row reached `DELETE_FLAG = DELETED`, and temporary DB artifacts were cleaned.

## Remaining High-Level Plan

| Phase | Planned Dates | Target | Deliverables | Exit Criteria |
| --- | --- | --- | --- | --- |
| 1. Progress audit and gap map | 2026-06-01 to 2026-06-02 | Freeze current gap list | `docs/tasks/api-gap-map.md`, updated dashboard | Frontend API files mapped to implemented, missing read, and deferred write groups |
| 2. Remaining read-only business APIs | 2026-06-03 to 2026-06-07 | Finish safe GET/list/detail coverage | Sale project reads, customer strategy, invoice/invoicing/reissue/follow-up/rating reads | Main old frontend pages can load read-only data |
| 3. Data-scope and permission tightening | 2026-06-08 to 2026-06-10 | Align RBAC/data scope closer to Java | Data-scope org expansion, permission middleware checks where safe | Protected reads match expected user/org access |
| 4. Low-risk write endpoints | 2026-06-11 to 2026-06-15 | Add isolated CRUD without heavy side effects | Master-data writes for selected modules after review | Transactional writes smoke tested; no stock/payment/workflow side effects yet |
| 5. Business transactional writes | 2026-06-16 to 2026-06-22 | Implement side-effect-heavy writes | Purchase, inventory, delivery, settlement, payment/refund/status flows | Transactions, rollback tests, state updates, event replacement notes |
| 6. Workflow write runtime | 2026-06-23 to 2026-06-28 | Implement approval actions | Start/approve/reject/cancel and business side-effect hooks | Workflow smoke passes on imported data |
| 7. Frontend adaptation | Starts now as a parallel track; deeper work 2026-06-29 to 2026-07-03 | Make Vue frontend work against ThinkPHP API | Baseline import, token/request/menu/permission adaptation, missing API wrappers, joint backend/frontend smoke | Main workflows usable from browser |
| 8. Test hardening | 2026-07-04 to 2026-07-07 | Stabilize full system | Route/API regression suite, syntax/namespace checks, negative auth tests | `composer install`, `php think`, `route:list`, lint, smoke all pass |
| 9. Deployment rehearsal | 2026-07-08 to 2026-07-10 | Prepare runtime deployment | Env checklist, `scripts/deployment-readiness.ps1`, Nginx/PHP/runtime permission notes, backup plan | Staging deployment checklist passes |
| 10. Final online data sync plan | After phase 9, with user confirmation | Sync production/online realtime data into final project | Data backup, sync direction, downtime/rollback plan | User confirms sync plan before any production data operation |

## Time Forecast

| Delivery Level | Estimated Remaining Time | Meaning |
| --- | --- | --- |
| Backend read-only demo | 5 to 7 working days | Login plus most pages can load read-only data |
| Internal test version | 12 to 18 working days | Core writes and workflow begin to work; frontend smoke starts |
| Production-ready candidate | 22 to 30 working days | Full regression, deployment rehearsal, and data-sync plan ready |

The estimate assumes continued small commits and local MySQL/Redis availability. Heavy workflow side effects, encrypted customer fields, and frontend mismatches are the largest uncertainty.

## Next Immediate Actions

1. Select the next complete feature-closure block only when it is backed by a Java-public route, an active visible frontend workflow, or an explicit user product request. The 2026-06-26 finance/purchase/inventory direct CRUD request opened payment/expenditure manual add-delete, collection/debit direct CRUD, purchase-order direct add-delete, and inventory zero-count delete; those blocks are complete. Task SSE, gen-config add, and non-`FOLLOW` product-item mutation still need the same module-specific transaction, permission, rollback, side-effect, and smoke-test plan before replacing wrappers with real behavior.
2. Prefer blocks with existing Java-compatible read coverage and clear side-effect boundaries before workflow runtime, provider sends, scheduler execution, tenant bootstrap, or production data sync. If no such block is ready, continue deployment hardening through `docs/tasks/deployment-runtime-readiness-plan.md`.
3. Keep using `docs/tasks/upload-provider-deferred-plan.md` for any additional render-only browser smoke on pages with upload, provider-send, or file-cleanup controls.
4. Use sub-agents for route-consumer reconnaissance and smoke-scope review before each new implementation slice.
5. Keep real cloud storage, customer encrypted-field strategy, and final production data sync deferred until their plans are approved.

## Frontend Joint Testing Rule

Frontend adaptation is now part of the active workflow, not a final-only task. Future backend slices should record whether they affect visible frontend pages. After the frontend baseline is imported, every completed route slice should be followed by a backend plus frontend smoke cycle:

1. Start MySQL/Redis.
2. Start ThinkPHP on port `82`.
3. Start Vue frontend on port `83`.
4. Browser-test login, menu loading, and the affected pages.
5. Record missing frontend calls in `docs/tasks/api-gap-map.md`.

Detailed workflow: `docs/tasks/frontend-joint-test-workflow.md`
